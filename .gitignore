# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.pins
# Note: Package.resolved IS committed (executable targets benefit from
# deterministic dep resolution across CI / consumer checkouts).

# Xcode
*.xcodeproj
DerivedData/
xcuserdata/

# Build artifacts
*.o
*.swiftmodule
*.dylib
*.so
*.a
build/

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.iml

# Python/tool caches
__pycache__/
*.py[cod]

# Test/runtime artifacts
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.db
*.db-wal
*.db-shm
*.rollout.jsonl
*.log
*.tmp
*.bak
codex-home-test/

# Local Codex state (never commit user data)
.codex/
codex_home/
.claude/
.maestro/
.playwright-mcp/

# Local environment
.env
.env.local

# Per-developer ephemeral
calc_0.py
scratch/
tmp/
catscan-output/
crm-build/
flightdeck/
crm-turn-*.png
crm-build.tar.gz
flightdeck.tar.gz

# Code signing / notarization secrets
*.p12
*.cer
*.provisionprofile
AuthKey_*.p8

# Node / web UI (www/) — regenerable deps + audit/trace artifacts (never commit)
node_modules/
.audits/
.turbo/
*.tsbuildinfo
