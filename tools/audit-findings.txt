
======================================================================
CRITICAL
======================================================================

[protocol-wire-types] ThreadItem.commandExecution `command` serialized as array instead of single joined string (wrong-field)
  RUST: app-server-protocol/src/protocol/v2/item.rs:248-251 `CommandExecution { id, command: String, cwd: AbsolutePathBuf, ... }`; item_builders.rs:71/91/118 build it via `command: shlex_join(&payload.command)` producing a single string e.g. "git status".
  SWIFT: Sources/ProtocolModel/Items.swift:20 declares `case commandExecution(id:, command: [String], ...)`; encode at Items.swift:126 `try c.encode(cmd, forKey: .command)` writes a JSON array; harness emits arrays at SessionEngine.swift:1319/1607/1621 (`command: [name]` / `command: [command]`).
  DESC: Upstream `item/started`/`item/completed` notifications carry `commandExecution.command` as a single string. Swift emits it as `["git","status"]`. A frontend decoding the documented `string` field will fail or mis-render the command for every shell command execution item.
  FIX: Change the wire representation of `commandExecution.command` to a single shlex-joined String to match upstream `command: String`.\n\nMinimal, surgical option (keep the associated value as [String] internally, fix only the wire shape):\n1. Add a shlex_join helper (port of codex-rs codex_shell_command::parse_command::shlex_join) into Sources/ProtocolModel/ — joins argv with spaces, single-quoting any token containing whitespace or shell metacharacters and escaping embedded quotes, matching Rust's shlex::try_join semantics.\n2. In Sources/ProtocolModel/Items.swift:126, change `try c.encode(cmd, forKey: .command)` to `try c.encode(shlexJoin(cmd), forKey: .command)`.\n3. In Items.swift:163 decode, change `command: try c.decode([String].self, forKey: .command)` to decode a `String` and wrap it as a single-element array (`command: [try c.decode(String.self, forKey: .command)]`) so the in-memory type is unchanged but the wire is a string.\n\nCleaner option (match upstream type exactly): change the enum associated value at Items.swift:20 from `command: [String]` to `command: String`, update the id/typeName/Codable switches, update ContextManager.swift:135/138/294 and Rollout usages, and at the three emit sites (SessionEngine.swift:1319/1607/1622) pass `shlexJoin([name])` / `shlexJoin([command])` (here the argv is already a single full command string, so the join is effectively identity but should still pass through shlexJoin for correct quoting). Also update ProtocolModelTests.swift:161 to expect `command: "ls -la"`.\n\nEither way, add a byte-faithful test asserting the emitted JSON for `commandExecution` contains `\"command\":\"ls -la\"` (string), not `[\"ls\",\"-la\"]`.

[app-server-events] turn/plan/updated emitted under wrong method name item/plan/updated (and never emitted) (wrong-casing)
  RUST: app-server-protocol/src/protocol/common.rs:1469 `TurnPlanUpdated => "turn/plan/updated" (v2::TurnPlanUpdatedNotification)`; emitted in app-server/src/bespoke_event_handling.rs:1248-1260 handle_turn_plan_update via ServerNotification::TurnPlanUpdated. Payload struct turn.rs:367-372 {threadId, turnId, explanation, plan}.
  SWIFT: Sources/ProtocolModel/Events.swift:222 `case .planUpdate: return "item/plan/updated"` — wrong method string. The case is defined (Events.swift:171) and has a wire body (Events.swift:296), but grep shows it is never emitted anywhere in Sources/ (no `emit(.planUpdate` / `.planUpdate(threadId:`).
  DESC: Upstream method is `turn/plan/updated`. Swift returns `item/plan/updated`, so a frontend subscribing to the upstream method would never receive plan updates. Compounded by the fact the case is dead code (never emitted by SessionEngine or RequestRouter).
  FIX: Two changes required:

1. Fix the method string in Sources/ProtocolModel/Events.swift:222:
   change `case .planUpdate: return "item/plan/updated"`
   to     `case .planUpdate: return "turn/plan/updated"`
   Also update the stale doc comments at Events.swift:439 ("`item/plan/updated`") to `turn/plan/updated`.

2. Wire the emitter so the case is no longer dead. In Sources/HarnessCore/SessionEngine.swift, where the turn dispatches a tool call (the same place ShellOutputBus is subscribed per turn), subscribe to PlanUpdateBus for the call's callId and forward the payload as an emitted event. Concretely, when dispatching an `update_plan` call:
   `await PlanUpdateBus.shared.subscribe(callId: call.callId) { [weak self] payloadJSON in`
   `    // decode payloadJSON via ProtocolModel.UpdatePlanArgs / PlanItemArg`
   `    self?.emit(.planUpdate(threadId: config.threadId, turnId: currentTurnId, explanation: args.explanation, plan: args.plan))`
   `}`
   and call `await PlanUpdateBus.shared.unsubscribe(callId:)` when the dispatch resolves — mirroring the existing ShellOutputBus subscribe/unsubscribe pattern referenced in ShellTool.swift:738-740. (The same gap likely applies to RequestUserInputBus / RequestPermissionsBus per that comment, but those are out of scope for this finding.)

[session-turn-loop] Interrupted turns emit a non-existent `turn/aborted` method instead of `turn/completed` with status "interrupted" (wrong-semantics)
  RUST: app-server/src/bespoke_event_handling.rs:1478-1500 `handle_turn_interrupted` maps EventMsg::TurnAborted to `emit_turn_completed_with_status(... status: TurnStatus::Interrupted ...)` which (1266-1293) sends `ServerNotification::TurnCompleted(TurnCompletedNotification{turn: Turn{status: Interrupted,...}})`. app-server-protocol/src/protocol/common.rs:1455-1495 lists every v2 notification method; there is NO `turn/aborted` method (grep confirmed exit 1). The only turn-terminal notification is `turn/completed` => "turn/completed" (common.rs:1466).
  SWIFT: Sources/ProtocolModel/Events.swift:146-149 defines a `.turnAborted` case whose method (Events.swift:218) is the string "turn/aborted"; Sources/HarnessCore/SessionEngine.swift:1816-1831 emits `.turnAborted(... reason ...)` for `status == .interrupted` and explicitly suppresses `turn/completed` in that branch. The TurnStatus enum already has `.interrupted` (Events.swift:15) which would serialize correctly inside a Turn object.
  DESC: Upstream never sends a `turn/aborted` JSON-RPC notification. An interrupted/aborted turn is delivered to clients as `turn/completed` carrying `turn.status:"interrupted"` (with completedAt/durationMs on the Turn). The Swift port invents a `turn/aborted` method and routes the interrupted reason + lastAgentMessage into it, so a frontend built against the real app-server protocol (which only subscribes to `turn/completed`) will never see the turn terminate and will treat the turn as perpetually in-progress.
  FIX: In Sources/HarnessCore/SessionEngine.swift (the terminal-emit block around lines 1816-1840), remove the `if status == .interrupted` branch that emits .turnAborted and instead ALWAYS emit .turnCompleted carrying a TurnObject whose status is the actual terminal status (.interrupted included), with completedAt/durationMs populated and error/lastAgentMessage handled as today. Upstream puts the interrupt reason nowhere on the turn/completed payload except via status=Interrupted; lastAgentMessage is not part of TurnCompletedNotification.Turn, so it should not be added to the wire (it can still be returned to internal callers separately if needed). Then update consumers: in Sources/HarnessCore/MultiAgent.swift:486-493, the .turnCompleted case already reads turn.status, so the .turnAborted case can be deleted (the .interrupted status now arrives via turnCompleted). Finally remove the now-unused .turnAborted enum case, its method() mapping to "turn/aborted" (Events.swift:218), the toMessage serialization (Events.swift:279-283), and the TurnAbortedBody struct (Events.swift:347-374) from Sources/ProtocolModel/Events.swift, so the port no longer exposes a non-existent JSON-RPC method. TurnStatus.interrupted (Events.swift:15) already serializes as "interrupted", so the turn/completed payload will match upstream.

[exec-unified-shell] Remote exec-server stdin write uses wrong method name (process/writeStdin vs process/write) (wrong-semantics)
  RUST: exec-server/src/protocol.rs:17 `pub const EXEC_WRITE_METHOD: &str = "process/write";`. Registered at exec-server/src/server/registry.rs:64 and used by the client at exec-server/src/client.rs:332. WriteParams (protocol.rs:126-131) has fields `processId` and `chunk` (a transparent base64 ByteChunk).
  SWIFT: Sources/Tools/RemoteExecServerTools.swift:835 sends `method: "process/writeStdin"` with params {processId, processHandle, deltaBase64, closeStdin}. No `process/write` exists anywhere in Sources/ (grep confirms only `process/writeStdin`).
  DESC: The upstream exec-server registers and accepts the stdin write under method `process/write` with body field `chunk` (base64). The Swift remote client invokes `process/writeStdin` with body fields `deltaBase64` and `closeStdin`. A real upstream exec-server would return method-not-found / invalid-params, so the remote unified-exec write path is completely broken against a faithful server.
  FIX: In /Users/chabotc/Projects/codex-swift/Sources/Tools/RemoteExecServerTools.swift, change the writeStdin() body (currently lines 835-840) to use the exec-server contract:

```swift
_ = try await client.request(method: "process/write", params: RemoteJSON([
    "processId": remoteProcessId,
    "chunk": Data(input.utf8).base64EncodedString(),
]), replay: .never)
```

Rationale: upstream EXEC_WRITE_METHOD is "process/write" (protocol.rs:17) and WriteParams is exactly {processId, chunk} with chunk a #[serde(transparent)] base64 ByteChunk (protocol.rs:128-131). Drop `processHandle`, `deltaBase64`, and `closeStdin` — none exist in the exec-server WriteParams. (If a stdin-close is ever needed, the exec-server has no closeStdin field on write; that would require a separate mechanism, but the current call passes closeStdin:false so dropping it is behavior-preserving.) Keep replay: .never. Optionally verify no other exec-server-client call sites send `deltaBase64`/`processHandle`.

[sandbox-safety-policy] Swift Seatbelt profile omits the entire upstream base SBPL policy (wrong-semantics)
  RUST: codex-rs/sandboxing/src/seatbelt.rs:20,711-722 always prepends MACOS_SEATBELT_BASE_POLICY (seatbelt_base_policy.sbpl) to every generated profile. That base policy (seatbelt_base_policy.sbpl:8-123) sets `(deny default)` and then grants a large required set: process-exec/fork, `(allow signal (target same-sandbox))`, `(allow process-info* (target same-sandbox))`, `(allow file-write-data (require-all (path "/dev/null") (vnode-type CHARACTER-DEVICE)))`, ~70 `sysctl-read` names, `(allow mach-lookup (global-name com.apple.system.opendirectoryd.libinfo))`, IOKit RootDomainUserClient, ipc-posix-sem/shm, `(allow pseudo-tty)` + /dev/ptmx + /dev/ttys ioctl rules, and `(allow user-preference-read)` + cfprefsd mach-lookups. When include_platform_defaults() it also appends restricted_read_only_platform_defaults.sbpl (seatbelt.rs:718-720).
  SWIFT: Sources/Sandbox/Sandbox.swift:364-420 buildSeatbeltProfile emits only `(version 1) (deny default) (allow process-fork) (allow process-exec) (allow file-read*)` plus writable-root allow clauses, protected-metadata deny regexes, and `(allow network*)`. None of the base-policy sysctl/mach-lookup/pseudo-tty/cfprefs/dev-null/IOKit allows are present, and no .sbpl resource files are bundled (confirmed: no *.sbpl under Sources/, only models.json in Resources/). This is the only profile path used (confinementProfile -> sandboxedInvocation -> ShellTool.swift:294 / UnifiedExec.swift:724,921).
  DESC: Under `(deny default)`, a sandboxed child launched via sandbox-exec with the Swift profile cannot read CPU/kernel sysctls, look up user/directory info (opendirectoryd), allocate POSIX semaphores/shared memory, use a PTY, or read user preferences. Upstream explicitly grants all of these because real tools (interactive shells, python multiprocessing, PyTorch/libomp, git, anything reading kern.* / hw.* sysctls) need them. The Swift sandbox will cause widespread tool failures (e.g. interactive shells not detecting a TTY, python multiprocessing SemLock failures) that do not occur upstream.
  FIX: In Sources/Sandbox/Sandbox.swift, prepend the upstream base policy to every generated profile so that under (deny default) the required system grants exist. Concretely:

1. Add a static constant holding the upstream seatbelt_base_policy.sbpl content. Best: copy codex-rs/sandboxing/src/seatbelt_base_policy.sbpl into Sources/Sandbox/Resources/seatbelt_base_policy.sbpl, add a Bundle resource to the Sandbox target in Package.swift (.process or .copy), and load it via Bundle.module; or, to avoid resource-bundling complexity, embed it as a Swift string literal constant (e.g. `static let macosSeatbeltBasePolicy = """ ... """`). Either way the text must match upstream verbatim (it already begins with (version 1) and (deny default)).

2. In buildSeatbeltProfile, replace the hand-rolled prefix [ "(version 1)", "(deny default)", "(allow process-fork)", "(allow process-exec)", "(allow file-read*)" ] with the base policy as the first section, mirroring upstream policy_sections ordering: base policy, then file-read policy, then file-write policy (writable-root allows), then deny-read/protected-metadata, then network policy. Since the base policy already emits (version 1), (deny default), process-exec, process-fork, signal, process-info, etc., drop those duplicates from the Swift-generated lines (keep (version 1) only once — it must appear exactly once and first).

3. Keep emitting the writable-root (allow file-write* (subpath ...)) clauses, the protected-metadata deny regexes, and (allow network*)/network policy AFTER the base policy text.

4. Optionally reproduce include_platform_defaults() by also bundling/embedding restricted_read_only_platform_defaults.sbpl and appending it when the policy is read-only with platform defaults enabled, to match upstream's restricted_read_only_platform_defaults appendix. (Lower priority since Swift currently uses a blanket (allow file-read*).)

Minimal patch shape:
  static func buildSeatbeltProfile(policy: SandboxPolicy, cwd: String) -> String {
      var sections = [macosSeatbeltBasePolicy]   // includes (version 1)(deny default)(process-exec)(process-fork)(signal)(process-info)(sysctl)(mach-lookup)(ipc)(pseudo-tty)(user-preference)...
      sections.append("(allow file-read*)")
      // ...existing writable-root allow + protected-metadata deny generation, collected into a string...
      if policy.networkAllowed { sections.append("(allow network*)") }
      return sections.joined(separator: "\n")
  }
Ensure the existing tests that assert the exact line set are updated to expect the base policy prefix.

[sandbox-safety-policy] Network-enabled Seatbelt profile uses blanket `(allow network*)` instead of upstream structured network policy (wrong-semantics)
  RUST: codex-rs/sandboxing/src/seatbelt.rs:307-315 emits `(allow network-outbound)\n(allow network-inbound)` and appends MACOS_SEATBELT_NETWORK_POLICY (seatbelt_network_policy.sbpl). That network policy (seatbelt_network_policy.sbpl:5-31) constrains system-socket to `(require-all (socket-domain AF_SYSTEM) (socket-protocol 2))` and grants only specific mach-lookups needed for TLS/DNS (com.apple.SecurityServer, com.apple.networkd, com.apple.ocspd, com.apple.trustd.agent, com.apple.SystemConfiguration.DNSConfiguration/configd, dirhelper, opendirectoryd.membership) plus `(allow sysctl-read (sysctl-name-regex #"^net.routetable"))`. When no network requested, no network rules are emitted at all (and codex-core sets CODEX_SANDBOX_NETWORK_DISABLED env var, core/src/sandboxing/mod.rs:133-138).
  SWIFT: Sources/Sandbox/Sandbox.swift:418 `if policy.networkAllowed { lines.append("(allow network*)") }`. There is no AF_SYSTEM restriction, no SecurityServer/networkd/ocspd/trustd/DNS mach-lookup grants, and no net.routetable sysctl grant.
  DESC: Two divergences: (1) `(allow network*)` is broader than upstream's network-outbound/inbound + restricted system-socket (AF_SYSTEM proto 2 only); the Swift profile would permit raw system sockets upstream denies. (2) Because the base policy is missing (see prior finding), even legitimate network usage that needs the TLS/DNS mach-lookups (com.apple.SecurityServer, networkd, trustd, DNSConfiguration) is not granted, so HTTPS/DNS resolution can still fail. The Swift profile is simultaneously broader (system-socket) and narrower (no service lookups) than the upstream contract.
  FIX: In Sources/Sandbox/Sandbox.swift, replace the line at 418 (`if policy.networkAllowed { lines.append("(allow network*)") }`) so that, when networkAllowed is true, it appends the upstream-equivalent structured block instead of a bare `(allow network*)`:

    if policy.networkAllowed {
        lines.append("(allow network-outbound)")
        lines.append("(allow network-inbound)")
        // Mirror codex-rs/sandboxing/src/seatbelt_network_policy.sbpl
        lines.append("(allow system-socket (require-all (socket-domain AF_SYSTEM) (socket-protocol 2)))")
        lines.append("""
        (allow mach-lookup
            (global-name \"com.apple.bsd.dirhelper\")
            (global-name \"com.apple.system.opendirectoryd.membership\")
            (global-name \"com.apple.SecurityServer\")
            (global-name \"com.apple.networkd\")
            (global-name \"com.apple.ocspd\")
            (global-name \"com.apple.trustd.agent\")
            (global-name \"com.apple.SystemConfiguration.DNSConfiguration\")
            (global-name \"com.apple.SystemConfiguration.configd\"))
        """)
        lines.append("(allow sysctl-read (sysctl-name-regex #\"^net.routetable\"))")
    }

This reproduces the BSD-socket grants (network-outbound/inbound rather than the looser network*), the restricted AF_SYSTEM proto-2 system-socket, the TLS/DNS mach-lookups, and the net.routetable sysctl-read. Ideally vendor seatbelt_network_policy.sbpl as a resource (matching upstream's include_str!) so the two stay in sync, and add a unit test asserting the network-enabled profile contains com.apple.SecurityServer and the AF_SYSTEM system-socket clause. Note the title's "broader on system-socket" claim should be dropped — only the missing-grants half is accurate.

[config] config/read origins has wrong shape (string layer name vs ConfigLayerMetadata object keyed by dotted path) (wrong-field)
  RUST: app-server-protocol/src/protocol/v2/config.rs:370-376 ConfigReadResponse.origins: HashMap<String, ConfigLayerMetadata>; ConfigLayerMetadata (lines 287-293) = { name: ConfigLayerSource (a #[serde(tag="type")] tagged enum e.g. {"type":"user",...}), version: String }. config/src/fingerprint.rs:8-33 record_origins inserts one entry per leaf keyed by the dotted path (path.join(".")) -> ConfigLayerMetadata.
  SWIFT: Sources/Supervisor/RequestRouter.swift:1742 emits "origins": .object(effectiveConfig.originsJSON()...); Sources/Config/Config.swift:174-178 originsJSON() returns {topLevelKey: .string(layerName)} — flat top-level keys, plain string values, no {name,version} object and no dotted-path leaves.
  DESC: Upstream origins maps every leaf config path (dotted) to a metadata object containing a tagged ConfigLayerSource and a version string. Swift maps only top-level keys to a bare layer-name string, so the value type (string vs object), the key granularity (top-level vs dotted leaf), and the ConfigLayerSource tagged-enum encoding are all wrong. A frontend reading origins["model"].name.type or origins["sandbox_workspace_write.network_access"] gets nothing usable.
  FIX: In Sources/Config/Config.swift: (1) Add a ConfigLayerSource value type encoding the upstream tagged enum — a struct/enum serializing to {"type":"user","file":...,"profile":...}, {"type":"system","file":...}, {"type":"project","dotCodexFolder":...}, {"type":"sessionFlags"}, {"type":"mdm","domain":...,"key":...} (camelCase keys, "type" tag). Map the existing layer names: "defaults"->(no upstream equivalent; upstream omits defaults from origins — only record origins for layers that come from a TOML source), "system"->{type:system,file}, "toml"->{type:user,file,profile}, "profile-v2"->folded into user with profile set, project layers->{type:project,dotCodexFolder}, "override"/session -c->{type:sessionFlags}. (2) Add a ConfigLayerMetadata = {name: ConfigLayerSource, version: String} where version is the canonical content hash (port config/src/fingerprint.rs version_for_toml: sha256 of canonical JSON of the layer's TOML). (3) Change Config.origins to [String: ConfigLayerMetadata] and replace the top-level loop with a recursive walk mirroring record_origins: descend into .object and .array values, and at each scalar leaf insert origins[path.joined(separator: ".")] = meta for the layer that last set it. (4) Rewrite originsJSON() to emit, per dotted leaf key, .object(["name": layerSource.toJSON(), "version": .string(version)]). (5) RequestRouter.swift:1742 then emits the correct nested shape unchanged. Also update GenericResponses.swift:256 stub if it must match (it currently returns {} which is a valid empty HashMap, so acceptable).

[prompts] Review prompt is paraphrased, not verbatim, and omits required output-schema fields (prompt-text)
  RUST: codex-rs/core/review_prompt.md (included via core/src/client_common.rs:19 `REVIEW_PROMPT`): full reviewer rubric starting `# Review guidelines:` with the JSON `## Output schema — MUST MATCH *exactly*` block requiring `overall_correctness: "patch is correct" | "patch is incorrect"`, `overall_explanation`, and `overall_confidence_score`. The contract struct codex-rs/protocol/src/protocol.rs:2998-3002 `ReviewOutputEvent { findings, overall_correctness, overall_explanation, overall_confidence_score }` mirrors these field names.
  SWIFT: Sources/Prompts/Templates.swift:572 `reviewPrompt` is a rewritten prompt (`You are acting as a meticulous code reviewer...`) whose JSON shape only lists `overall_explanation`, `findings[]`, `title/body/confidence_score/priority/code_location` — it omits `overall_correctness` and `overall_confidence_score` entirely and uses different schema wording. Used at Sources/HarnessCore/SessionEngine.swift:1656 as `baseInstructionsOverride`.
  DESC: Upstream sends a long, specific reviewer rubric verbatim and instructs the model to emit `overall_correctness` and `overall_confidence_score`; Swift sends a much shorter paraphrase that instructs a different/incomplete JSON schema. A frontend (or any consumer) expecting the upstream review JSON contract will get model output missing the `overall_correctness`/`overall_confidence_score` fields, and the reviewer behavior (priority tagging guidelines, bug-flagging criteria, suggestion-block rules) differs from upstream.
  FIX: Make the Swift review prompt verbatim and the schema complete. Concrete steps: (1) Copy /Users/chabotc/Projects/codex/codex-rs/core/review_prompt.md verbatim into Sources/Prompts/Resources/ (e.g. review_prompt.md) and load it as a bundled resource, OR replace the body of Templates.swift `reviewPrompt` (line 572-592) with the exact upstream text including the `# Review guidelines:` rubric and the full `## Output schema — MUST MATCH *exactly*` JSON block. (2) Ensure the instructed JSON includes the two missing required fields: add `"overall_correctness": "patch is correct" | "patch is incorrect"` and `"overall_confidence_score": <float 0.0-1.0>` at the top level alongside `overall_explanation` and `findings[]`. (3) For full fidelity, add a ReviewOutputEvent Codable struct to Sources/ProtocolModel (mirroring codex-rs protocol.rs:2998 ReviewFinding/ReviewOutputEvent with CodingKeys findings, overall_correctness, overall_explanation, overall_confidence_score) and parse the reviewer model's finalText in SessionEngine.runReview (around line 1696) before rendering, matching upstream tasks/review.rs::parse_review_output_event (try direct JSON decode, then a brace-slice fallback). At minimum, step (1)+(2) close the prompt-text and instructed-schema divergence.

[mcp] Streamable-HTTP transport never sends Mcp-Session-Id header (wrong-semantics)
  RUST: rmcp-0.15.0/src/transport/common/http_header.rs:1 `HEADER_SESSION_ID: &str = "Mcp-Session-Id"`; reqwest/streamable_http_client.rs:37,78,104-105 add the session id header to every POST/GET/DELETE, and :131-138 read `Mcp-Session-Id` from the initialize/post response. rmcp_client.rs uses StreamableHttpClientTransport for all HTTP servers.
  SWIFT: Sources/MCP/McpHttpClient.swift:168-200 (curlArgs) sets only Content-Type, Accept, Authorization, static and env http headers — never Mcp-Session-Id. The streaming reader parseStatusAndContentType (634-658) extracts only the status code and Content-Type; the response's Mcp-Session-Id is discarded and never threaded into subsequent requests.
  DESC: Upstream's Streamable-HTTP MCP client captures the Mcp-Session-Id returned on initialize and includes it on every subsequent request (tools/list, tools/call, resources/read, notifications, server-request replies). Swift sends no session header on any HTTP request and never parses it from the response. Any MCP server that establishes a session (the codex_apps connectors server and most stateful Streamable-HTTP servers do — the 404 session-expiry recovery path in both codebases exists precisely because sessions are stateful) will reject every post-initialize request, typically with 400/404, making such servers unusable.
  FIX: In Sources/MCP/McpHttpClient.swift: (1) Add actor state `private var sessionId: String?`. (2) Extend the streaming reader to surface response headers: change parseStatusAndContentType to also return the `Mcp-Session-Id` header value (case-insensitive match, do not `break` after content-type), and emit it on the `.head` event (add a `sessionId: String?` field to HttpStreamEvent.head). (3) In processStreamingResponse, when a head event carries a non-nil session id, store it on the actor (`self.sessionId = sid`) — capturing it from the initialize response and any later response that issues/rotates one. (4) In curlArgs, after the static/env headers, append `if let sid = sessionId { args += ["-H", "Mcp-Session-Id: \(sid)"] }` so it is sent on every POST (tools/list, tools/call, resources/read, notifications, and server-request replies via replyToServerRequest, which already uses curlArgs). (5) On reinitialize()/markUninitialized, clear `sessionId = nil` so the fresh handshake re-captures it. Optionally add a DELETE-on-stop with the session id to mirror upstream delete_session, though that is lower priority than the request-path header.

[hooks] hook/started and hook/completed notifications are not emitted at all (missing-notification)
  RUST: app-server-protocol/src/protocol/common.rs:1465 `HookStarted => "hook/started" (v2::HookStartedNotification)` and :1467 `HookCompleted => "hook/completed" (v2::HookCompletedNotification)`; app-server/src/bespoke_event_handling.rs:982-1001 maps `EventMsg::HookStarted`/`HookCompleted` into `ServerNotification::HookStarted`/`HookCompleted` carrying `{thread_id, turn_id, run}`; core/src/hook_runtime.rs:477 & 494 emit these EventMsgs per hook run.
  SWIFT: ABSENT. `grep -rni '"hook/' Sources/` returns nothing; no `HookStarted`/`HookCompleted` symbols exist outside doc comments. SessionEngine fires hooks via `hooks.fire(...)` (Sources/HarnessCore/SessionEngine.swift:1877,1899,1915,1943,2007,2037,2055,2078) and only consumes the returned `[HookOutcome]` for decisions; it never produces or sends a per-run notification.
  DESC: Upstream emits a `hook/started` notification before each hook handler runs and a `hook/completed` notification after, each containing a full `HookRunSummary`. These are first-class client-facing v2 notifications a frontend uses to render live hook-execution rows (status transitions Running -> Completed/Failed/Blocked/Stopped, timing, output entries). The Swift port emits neither, so any frontend hook-activity UI is permanently empty.
  FIX: Add the two notifications to the Swift v2 surface and emit them around each hook run:

1. Sources/ProtocolModel/Events.swift — add a Swift mirror of v2::HookRunSummary (fields: id, eventName, handlerType, executionMode, scope, sourcePath, source, displayOrder, status, statusMessage, startedAt, completedAt, durationMs, entries [{kind, text}]) as a Codable/Equatable struct, then add two ServerNotification cases:
   case hookStarted(threadId: ThreadId, turnId: TurnId?, run: HookRunSummary)
   case hookCompleted(threadId: ThreadId, turnId: TurnId?, run: HookRunSummary)
   Add to the method map: case .hookStarted: return "hook/started"; case .hookCompleted: return "hook/completed". Add the matching serialization arms in toMessage(...) producing {threadId, turnId, run} (camelCase, omit turnId when nil to match Option<String>).

2. Sources/HarnessCore/Hooks.swift — extend HookEngine.fire(...) (or the per-handler execution loop it calls) to capture per-run metadata so it can produce a HookRunSummary per handler: a stable run id, the resolved handlerType/executionMode/scope/sourcePath/source/displayOrder for the matched handler, startedAt/completedAt/durationMs timestamps, the final HookRunStatus (Running before exec is conceptual; on return one of Completed/Failed/Blocked/Stopped derived from HookOutcome.decision/shouldStop/shouldBlock/outputSchemaError), and entries built from stdout/stderr/outputSchemaError. Expose these summaries to the caller — e.g. change fire to return ([HookOutcome], [HookRunSummary]) or add a callback, OR have HookEngine accept an emit closure injected by SessionEngine.

3. Sources/HarnessCore/SessionEngine.swift — in each fireXxxHook helper (1875-2078), before running the handlers emit ServerNotification.hookStarted(threadId:, turnId:, run:) for each preview run, and after completion emit ServerNotification.hookCompleted(threadId:, turnId:, run:) for each finished run via the existing self.emit(...) path (line 370), mirroring upstream emit_hook_started_events / emit_hook_completed_events ordering (started before, completed after, one per run). turnId is the current TurnId where available (nil for session-scoped hooks like SessionStart).

[hooks] HookRunSummary payload + nested types entirely missing (missing-type)
  RUST: app-server-protocol/src/protocol/v2/hook.rs:96-115 `HookRunSummary` (`#[serde(rename_all="camelCase")]`) with fields id, eventName, handlerType, executionMode, scope, sourcePath, source, displayOrder, status, statusMessage, startedAt, completedAt, durationMs, entries; :82-85 `HookOutputEntry {kind,text}`; :141-154 the two notification structs all carry `run: HookRunSummary`.
  SWIFT: ABSENT. No `HookRunSummary`/`HookOutputEntry`/`HookStartedNotification`/`HookCompletedNotification` struct exists in Sources/ (grep confirms only doc-comment mentions in Hooks.swift). Swift `HookOutcome` (Hooks.swift:213) is an internal decision struct with none of these wire fields.
  DESC: The wire payload that both hook notifications carry (`HookRunSummary` with its camelCase fields and the `HookOutputEntry{kind,text}` list) has no Swift representation. Without it the notifications above cannot be constructed even if dispatch were added.
  FIX: Add the wire types and their emission. Concretely:

1) In Sources/ProtocolModel/ (e.g. a new Hook.swift or alongside Events.swift), add Codable structs mirroring v2/hook.rs with camelCase CodingKeys (the default for these simple names):
   - enums HookEventName, HookHandlerType, HookExecutionMode, HookScope, HookSource (default .unknown when absent), HookRunStatus, HookOutputEntryKind — each String-rawValue, camelCase (e.g. preToolUse, postToolUse, preCompact...).
   - struct HookOutputEntry { kind: HookOutputEntryKind; text: String }
   - struct HookRunSummary { id; eventName; handlerType; executionMode; scope; sourcePath: String; source (decodeIfPresent, default .unknown); displayOrder: Int; status; statusMessage: String?; startedAt: Int; completedAt: Int?; durationMs: Int?; entries: [HookOutputEntry] }
   - struct HookStartedNotification { threadId: String; turnId: String?; run: HookRunSummary }
   - struct HookCompletedNotification { threadId: String; turnId: String?; run: HookRunSummary }
   Use encodeIfPresent for the optional fields (turnId, statusMessage, completedAt, durationMs) to match Rust's Option serialization.

2) Wire emission: in SessionEngine.swift's hook execution path (the section around lines 1849+ / wherever HookEngine.run is invoked), build a HookRunSummary before running each hook (status .running -> emit hook/started) and after (status from outcome, populate completedAt/durationMs/entries derived from HookOutcome.outputSchemaError -> HookOutputEntry(kind: .error) etc. -> emit hook/completed). Add the corresponding cases to the event enum in Sources/ProtocolModel/Events.swift and route them to JSON-RPC notification methods "hook/started" and "hook/completed" in Sources/Supervisor/RequestRouter.swift (matching upstream bespoke_event_handling.rs:982-1001), populating threadId/turnId/run.

======================================================================
MAJOR
======================================================================

[protocol-wire-types] ThreadItem.reasoning `summary` is a single string, not an array; `content` array missing (wrong-field)
  RUST: app-server-protocol/src/protocol/v2/item.rs:239-245 `Reasoning { id, summary: Vec<String> (#[serde(default)]), content: Vec<String> (#[serde(default)]) }` (camelCase) — wire shape `{"type":"reasoning","id":...,"summary":[...],"content":[...]}`.
  SWIFT: Sources/ProtocolModel/Items.swift:19 `case reasoning(id:, summary: String)`; encode Items.swift:122 writes `summary` as a scalar string and never emits `content`.
  DESC: Upstream reasoning items carry `summary` and `content` as arrays of strings. Swift emits `summary` as a scalar and omits `content` entirely, so reasoning items deserialize incorrectly on the client.
  FIX: In Sources/ProtocolModel/Items.swift:
1. Change the enum case (line 19) to: `case reasoning(id: ItemId, summary: [String], content: [String])`.
2. Add `content` to the CodingKeys enum `K` (line 88): add `content` to the case list.
3. In `id` (line 61) and `typeName` switches, update the `.reasoning(let i, _)` pattern to `.reasoning(let i, _, _)`.
4. Encode (line 119-122): change to
   `case .reasoning(let id, let summary, let content):` then `try c.encode("reasoning", forKey: .type); try c.encode(id, forKey: .id); try c.encode(summary, forKey: .summary); try c.encode(content, forKey: .content)`. (Optionally always-encode both even when empty to mirror upstream serialization, which does emit empty arrays.)
5. Decode (line 159): change to
   `self = .reasoning(id: id, summary: try c.decodeIfPresent([String].self, forKey: .summary) ?? [], content: try c.decodeIfPresent([String].self, forKey: .content) ?? [])` — using decodeIfPresent with `[]` default to honor upstream `#[serde(default)]`.
6. Update the two callers in Sources/Persistence/Rollout.swift (lines 719, 964) and Sources/HarnessCore/ContextManager.swift:293 to the new arity. For Rollout.swift:964 build summary as the array of part texts (`summaries.compactMap { $0["text"] as? String }`) and content as `[]` (or from raw_content if available) instead of joining into one string; for the encode at line 719 emit the array(s).

[protocol-wire-types] item/started and item/completed omit required startedAtMs / completedAtMs (wrong-field)
  RUST: app-server-protocol/src/protocol/v2/item.rs:1059-1066 `ItemStartedNotification { item, thread_id, turn_id, started_at_ms: i64 }` and item.rs:1133-1140 `ItemCompletedNotification { item, thread_id, turn_id, completed_at_ms: i64 }` — both timestamp fields are plain required `i64` (no skip/default).
  SWIFT: Sources/ProtocolModel/Events.swift:372 `struct ItemEnvelope { var threadId; var turnId; var item }` is used for BOTH `.itemStarted` and `.itemCompleted` (Events.swift:266,276); no `startedAtMs`/`completedAtMs` field is ever emitted.
  DESC: Both lifecycle notifications require a millisecond timestamp upstream; Swift never sends it. A strict client decoder expecting `startedAtMs`/`completedAtMs` will reject the notification, and timeline rendering loses the timestamp.
  FIX: 1. Extend the enum cases in Sources/ProtocolModel/Events.swift:132 and :137 to carry a timestamp:
   `case itemStarted(threadId: ThreadId, turnId: TurnId, item: ThreadItem, startedAtMs: Int64)`
   `case itemCompleted(threadId: ThreadId, turnId: TurnId, item: ThreadItem, completedAtMs: Int64)`
2. Split ItemEnvelope into two wire structs (or add the field) in Events.swift:372:
   `struct ItemStartedEnvelope: Codable { var item: ThreadItem; var threadId: ThreadId; var turnId: TurnId; var startedAtMs: Int64 }`
   `struct ItemCompletedEnvelope: Codable { var item: ThreadItem; var threadId: ThreadId; var turnId: TurnId; var completedAtMs: Int64 }`
   (Int64 encodes to a JSON number, matching upstream i64.)
3. Update the serialization sites Events.swift:265-266 and :275-276 to build the new envelopes with the timestamp.
4. At each of the ~14 emit sites in Sources/HarnessCore/SessionEngine.swift (881, 1032, 1128, 1199, 1312, 1318, 1433, 1536, 1606, 1629, 1645, 1708, etc.) and MultiAgent.swift pattern matches, supply the timestamp via a helper mirroring upstream now_unix_timestamp_ms(), e.g. `Int64(Date().timeIntervalSince1970 * 1000)`. A shared `nowUnixMs()` helper is the cleanest approach. Note the millisecond granularity differs from the existing turn-level startedAtUnix (Int seconds at SessionEngine.swift:1055) — do not reuse that value directly.

[protocol-wire-types] item/reasoning/textDelta omits required contentIndex (and summaryTextDelta would need summaryIndex) (wrong-field)
  RUST: app-server-protocol/src/protocol/v2/item.rs:1200-1207 `ReasoningTextDeltaNotification { thread_id, turn_id, item_id, delta, content_index: i64 }` (required); item.rs:1177-1184 `ReasoningSummaryTextDeltaNotification { ... summary_index: i64 }`.
  SWIFT: Sources/ProtocolModel/Events.swift:213 `.reasoningDelta` returns method `item/reasoning/textDelta` but reuses the generic `Delta` struct (Events.swift:373-375 `{threadId,turnId,itemId,delta}`) which has no `contentIndex`. There is no `summaryTextDelta`/`summaryPartAdded` case at all.
  DESC: Upstream `item/reasoning/textDelta` requires `contentIndex`; Swift omits it. Additionally the summary-text-delta and summary-part-added notifications are not modeled, so reasoning summary streaming cannot be reproduced.
  FIX: In Sources/ProtocolModel/Events.swift:

1) Add contentIndex to the reasoning text-delta case and stop sharing the generic Delta struct for it:
   - Change case: `case reasoningDelta(threadId: ThreadId, turnId: TurnId, itemId: ItemId, delta: String, contentIndex: Int)`
   - Remove .reasoningDelta from the combined encode arm at lines 267-271 (keep agentMessageDelta + commandOutputDelta sharing `Delta`).
   - Add a dedicated encode arm and a new Codable struct:
       struct ReasoningDelta: Codable { var threadId: ThreadId; var turnId: TurnId; var itemId: ItemId; var delta: String; var contentIndex: Int }
       case .reasoningDelta(let tid, let turnId, let itemId, let delta, let contentIndex):
           params = try JSONBridge.value(ReasoningDelta(threadId: tid, turnId: turnId, itemId: itemId, delta: delta, contentIndex: contentIndex))

2) Add the two missing summary notifications:
   - case summaryTextDelta(threadId: ThreadId, turnId: TurnId, itemId: ItemId, delta: String, summaryIndex: Int) -> method "item/reasoning/summaryTextDelta"
   - case summaryPartAdded(threadId: ThreadId, turnId: TurnId, itemId: ItemId, summaryIndex: Int) -> method "item/reasoning/summaryPartAdded"
   with matching Codable structs:
       struct SummaryTextDelta: Codable { var threadId; var turnId; var itemId; var delta: String; var summaryIndex: Int }
       struct SummaryPartAdded: Codable { var threadId; var turnId; var itemId; var summaryIndex: Int }
   and add their cases to the `method` switch (lines 203-237) and the `toMessage` switch.

3) Update all call sites that construct .reasoningDelta to supply contentIndex (typically 0 for single-content reasoning streams), and wire the new summary notifications wherever reasoning summary deltas are produced.

[protocol-wire-types] model/list ModelInfo omits required supportedReasoningEfforts and defaultReasoningEffort (missing-type)
  RUST: app-server-protocol/src/protocol/v2/model.rs `pub struct Model { id, model, upgrade: Option, upgrade_info: Option, availability_nux: Option, display_name, description, hidden, supported_reasoning_efforts: Vec<ReasoningEffortOption>, default_reasoning_effort: ReasoningEffort, input_modalities (default), supports_personality (default), additional_speed_tiers (default), service_tiers (default), is_default }`. `supported_reasoning_efforts` and `default_reasoning_effort` have no serde default → required.
  SWIFT: Sources/ProtocolModel/V2.swift:213-225 `ModelInfo { id, model, displayName, description, hidden, isDefault }` only; emitted at Sources/Supervisor/RequestRouter.swift:1712-1714 for the typed `.modelList` case.
  DESC: The `model/list` response (a method real clients call) is emitted with `ModelInfo` lacking the required `supportedReasoningEfforts` (array) and `defaultReasoningEffort` fields. A client decoding upstream's `Model` type will fail to parse, and reasoning-effort UI cannot be populated.
  FIX: 1) In /Users/chabotc/Projects/codex-swift/Sources/ProtocolModel/V2.swift add a `ReasoningEffortOption` type and a `ReasoningEffort` enum (string-raw, camelCase JSON values: none, minimal, low, medium, high, xhigh — matching upstream lowercase serde; note `xhigh` serializes lowercase). Then extend `ModelInfo` to include `public var supportedReasoningEfforts: [ReasoningEffortOption]` and `public var defaultReasoningEffort: ReasoningEffort`, updating its init. Optionally also add the serde-defaulted fields (inputModalities default [text,image], supportsPersonality=false, additionalSpeedTiers=[], serviceTiers=[], upgrade/upgradeInfo/availabilityNux optionals) for full fidelity, but the two non-defaulted ones are mandatory. 2) In /Users/chabotc/Projects/codex-swift/Sources/Tokenizer/ModelCatalog.swift add supportedReasoningEfforts and defaultReasoningEffort metadata to ModelEntry (e.g. reasoning models advertise [low,medium,high]; default medium). 3) In /Users/chabotc/Projects/codex-swift/Sources/Supervisor/RequestRouter.swift:1714 populate the new fields from the catalog entry in the ModelInfo(...) construction.

[protocol-wire-types] Server-request CommandApprovalParams uses command:[String]/cwd:String and omits startedAtMs/approvalId (wrong-field)
  RUST: app-server-protocol/src/protocol/v2/item.rs:1256-1311 `CommandExecutionRequestApprovalParams { thread_id, turn_id, item_id, started_at_ms: i64 (required), approval_id: Option, reason: Option, command: Option<String> (single string), cwd: Option<AbsolutePathBuf>, command_actions: Option<Vec<CommandAction>>, ... }`.
  SWIFT: Sources/ProtocolModel/ServerRequest.swift:8-20 `CommandApprovalParams { threadId, turnId, itemId, command: [String], cwd: String, reason: String? }` — `command` is an array (upstream is a single optional string), `cwd` required (upstream optional), and required `startedAtMs` plus `approvalId` are absent.
  DESC: The outbound `item/commandExecution/requestApproval` server request differs from upstream: `command` is an array vs single string, and the required `startedAtMs` field is missing, so clients parsing the documented approval params shape mis-decode the command and lack the timestamp.
  FIX: Edit Sources/ProtocolModel/ServerRequest.swift CommandApprovalParams to match upstream: change `command: [String]` to `command: String?`, change `cwd: String` to `cwd: String?`, add `startedAtMs: Int64` (required) and `approvalId: String?`. Order/optionality: threadId, turnId, itemId, startedAtMs: Int64, approvalId: String? = nil, reason: String? = nil, command: String? = nil, cwd: String? = nil. Update both call sites in Sources/HarnessCore/SessionEngine.swift (lines ~578 and ~597): pass `command:` as a single joined string (e.g. argv.joined(separator: " ") or the original command-line string rather than the argv array), pass `cwd: config.cwd` (now optional), and supply `startedAtMs:` as the current Unix time in milliseconds (Int64(Date().timeIntervalSince1970 * 1000)). For Codable optionality parity with upstream serde skip_serializing_if=Option::is_none, the default Codable omission of nil optionals is acceptable. (Out of scope but recommended: apply the same startedAtMs addition to PatchApprovalParams to match FileChangeRequestApprovalParams.)

[app-server-registry] Experimental-API gate is missing many whole-method experimental methods (wrong-semantics)
  RUST: app-server-protocol/src/protocol/common.rs macro entries marked #[experimental(...)] (lines ~437-1055): thread/increment_elicitation, thread/decrement_elicitation, thread/goal/set, thread/goal/get, thread/goal/clear, thread/realtime/start|appendAudio|appendText|stop|listVoices, remoteControl/enable|disable|status/read, collaborationMode/list, fuzzyFileSearch/sessionStart|sessionUpdate|sessionStop. Gate enforced in message_processor.rs:792-796 -> invalid_request(experimental_required_message(reason)) (=-32600 'X requires experimentalApi capability'), verified by experimental_api.rs test asserting code -32600.
  SWIFT: Sources/WireProtocol/ExperimentalGate.swift:68-80 defaultMethods only lists 11 methods (thread/turns/list, thread/turns/items/list, thread/memoryMode/set, memory/reset, thread/backgroundTerminals/clean, process/spawn|writeStdin|kill|resizePty, mock/experimentalMethod, environment/add). The ~18 methods above are absent, so RequestRouter.swift:1378 gate.rejectionDescriptor never rejects them; they are dispatched directly (e.g. collaborationModeList at RequestRouter.swift:1784, threadGoalSet at 1601, realtime at 2192).
  DESC: Upstream rejects these methods for a connection that did not negotiate experimentalApi with -32600 'X requires experimentalApi capability'. Swift processes them normally, returning a success response. A frontend that probes capability gating, or that relies on the server to reject experimental methods when experimentalApi was not requested, observes the opposite behavior.
  FIX: In Sources/WireProtocol/ExperimentalGate.swift, extend `defaultMethods` (lines 68-80) to include all upstream experimental request methods. Add these 17 entries to the set:
  "thread/increment_elicitation",
  "thread/decrement_elicitation",
  "thread/goal/set",
  "thread/goal/get",
  "thread/goal/clear",
  "thread/realtime/start",
  "thread/realtime/appendAudio",
  "thread/realtime/appendText",
  "thread/realtime/stop",
  "thread/realtime/listVoices",
  "remoteControl/enable",
  "remoteControl/disable",
  "remoteControl/status/read",
  "collaborationMode/list",
  "fuzzyFileSearch/sessionStart",
  "fuzzyFileSearch/sessionUpdate",
  "fuzzyFileSearch/sessionStop",

This is sufficient because the gate at RequestRouter.swift:1378 runs before any of these methods reach their handlers (the typed dispatch at 1399+ and the string-handler fallthrough chain at 1825-1891). With the methods present in defaultMethods, rejectionDescriptor returns the method name when caps.experimentalApi is false, producing WireError.experimental (-32600, '<method> requires experimentalApi capability'), matching upstream message_processor.rs:792-796. Keep the existing helper handlers unchanged. Recommend deriving this list from the single source of truth (the ClientRequest method list) to avoid future drift, and adding a test mirroring upstream experimental_api.rs asserting -32600 for each newly-gated method when experimentalApi is not negotiated.

[app-server-registry] Experimental field-level gate (defaultFields) is materially incomplete (wrong-semantics)
  RUST: Field #[experimental("...")] annotations in app-server-protocol/src/protocol/v2/thread.rs (thread/start.permissions, .dynamicTools, .experimentalRawEvents, .persistFullHistory, .activePermissionProfile; thread/resume.history, .path, .runtimeWorkspaceRoots, .permissions, .persistFullHistory, .activePermissionProfile; thread/fork.path, .runtimeWorkspaceRoots, .permissions, .persistFullHistory, .activePermissionProfile), turn.rs (turn/start.responsesapiClientMetadata, .permissions, .collaborationMode; turn/steer.responsesapiClientMetadata), account.rs:66 (account/login/start.chatgptAuthTokens), command_exec.rs:107 (command/exec.permissionProfile).
  SWIFT: Sources/WireProtocol/ExperimentalGate.swift:81-89 defaultFields lists only 7 keys: thread/start.mockExperimentalField, thread/start.runtimeWorkspaceRoots, thread/start.environments, thread/resume.excludeTurns, thread/fork.excludeTurns, turn/start.runtimeWorkspaceRoots, turn/start.environments. The ~20 other experimental fields above are absent.
  DESC: A non-experimental client that passes any of the unlisted experimental params (e.g. thread/start.permissions, account/login/start.chatgptAuthTokens, command/exec.permissionProfile) is rejected with -32600 upstream but accepted/processed by Swift.
  FIX: In Sources/WireProtocol/ExperimentalGate.swift, extend defaultFields to mirror upstream using the WIRE (camelCase) keys, not the reason-string suffixes. Add:
"thread/start.permissions", "thread/start.dynamicTools", "thread/start.experimentalRawEvents", "thread/start.persistExtendedHistory",
"thread/resume.history", "thread/resume.path", "thread/resume.runtimeWorkspaceRoots", "thread/resume.permissions", "thread/resume.persistExtendedHistory", "thread/resume.activePermissionProfile",
"thread/fork.path", "thread/fork.runtimeWorkspaceRoots", "thread/fork.permissions", "thread/fork.persistExtendedHistory", "thread/fork.activePermissionProfile",
"turn/start.responsesapiClientMetadata", "turn/start.permissions", "turn/start.collaborationMode",
"turn/steer.responsesapiClientMetadata",
"account/login/start.chatgptAuthTokens",
"command/exec.permissionProfile".
Note: use "persistExtendedHistory" (the serde wire key) because presentFields() matches JSON keys; the upstream error descriptor text is "persistFullHistory" but the gate must key on the wire field. If exact-descriptor parity in the error message is also desired, add a wire-key->descriptor alias map so the rejection string reports "...persistFullHistory" while matching the incoming "persistExtendedHistory" key. Verify each wire key against the corresponding ProtocolModel CodingKeys to ensure they match the actual decoded JSON names.

[app-server-registry] turn/start does not enforce input-length limit; inputTooLarge helper has wrong code and shape (missing-error-code)
  RUST: request_processors/turn_processor.rs:309-327: input_too_large_error uses invalid_params(...) (code -32602) with message 'Input exceeds the maximum length of {MAX_USER_INPUT_TEXT_CHARS} characters.' and data {input_error_code:'input_too_large', max_chars, actual_chars}; validate_v2_input_limit rejects when summed text_char_count > MAX_USER_INPUT_TEXT_CHARS (=1<<20, protocol/src/user_input.rs:9). error_code.rs:8 INPUT_TOO_LARGE_ERROR_CODE is the string 'input_too_large', not a numeric JSON-RPC code.
  SWIFT: RequestRouter.swift:1628-1666 turnStart performs no input-length validation before submitting. Sources/WireProtocol/WireErrors.swift:11,40-43 defines inputTooLarge with numeric code -32000 and message 'input too large (limit N bytes)' and no data; this helper is never called on the dispatch path (no references found in Sources/ outside its definition).
  DESC: Swift never returns the input-too-large error for oversized turn input, and the only helper that exists uses the wrong JSON-RPC code (-32000 vs -32602), wrong message text, byte-based instead of char-based limit, and omits the data envelope (input_error_code/max_chars/actual_chars) that a client may surface.
  FIX: In Sources/Supervisor/RequestRouter.swift, add char-count validation at the top of both the .turnStart and .turnSteer cases, before calling supervisor.submit. Concretely: (1) Add a constant maxUserInputTextChars = 1 << 20 (1048576) in a shared protocol/config location (e.g. ProtocolModel or InfraPrimitives/Limits.swift). (2) Compute actualChars by summing the Swift character count (use Rust char semantics: Unicode scalar count to match Rust's char-based count — i.e. .unicodeScalars.count — over the text portions of each input item in p.input, matching V2UserInput::text_char_count which counts only text items). (3) If actualChars > maxUserInputTextChars, send a JSON-RPC error and return without submitting. (4) Fix/replace WireError.inputTooLarge to emit the upstream shape: code -32602, message "Input exceeds the maximum length of \(maxUserInputTextChars) characters.", and a data field {"input_error_code":"input_too_large","max_chars":maxUserInputTextChars,"actual_chars":actualChars}. This requires the JSONRPCError error envelope to support a `data` field (verify JSONRPCError supports optional data; if not, extend it). Example for turnStart: `let actualChars = p.input.reduce(0) { $0 + $1.textCharCount }; if actualChars > maxUserInputTextChars { await conn.send(WireError.inputTooLarge(id: id, actualChars: actualChars)); return }`. Apply the same guard to turnSteer (p.input) to match upstream's two call sites. Also confirm whether thread/start carries input requiring the same check (upstream only calls it at the two turn_processor sites, so turn/start + turn/steer suffice).

[app-server-registry] Turn-transition cancellation of pending server->client requests is not implemented (missing-notification)
  RUST: outgoing_message.rs:176-191 abort_pending_server_requests cancels all pending server requests for a thread with internal_error (-32603) 'client request resolved because the turn state was changed' and data {reason:'turnTransition'} (server_request_error.rs:3); thread_lifecycle.rs:721-744 resolve_pending_server_request emits ServerNotification::ServerRequestResolved{threadId,requestId}. Called on turn end/interrupt from bespoke_event_handling.rs:151,181,1113 and thread_lifecycle.rs:494.
  SWIFT: SessionSupervisor.swift:65,131-145,175 tracks pendingServerRequests and only clears them on thread close (clearThread:175). No code emits serverRequest/resolved on turn transition and no code fails pending server requests with the turnTransition -32603 error (grep for 'turnTransition'/'turn state was changed'/'serverRequestResolved' in Supervisor/HarnessCore returns nothing).
  DESC: When a turn ends/interrupts while approval/elicitation server->client requests are still pending, upstream proactively fails those pending requests (-32603, data.reason='turnTransition') and notifies the client with serverRequest/resolved. Swift leaves them dangling until the thread closes, so a frontend awaiting an approval response never receives the cancellation/resolution.
  FIX: Reproduce upstream abort_pending_server_requests on turn transitions in the worker (the holder of the awaiting continuations), since the Swift architecture keeps the awaiter in WorkerRuntime/WorkerServerRequestBroker rather than the supervisor.

1. In Sources/SessionWorkerCore/WorkerRuntime.swift, have the engine-event relay loop (the `for await n in events` block around line 134-139) observe turn-transition notifications. On `.turnCompleted` and `.turnAborted` (and optionally `.turnStarted`), call a new abortPendingServerRequests() that: (a) resumes all WorkerRuntime.pendingApprovals continuations with .cancel and cancels their timeouts (reuse/extend cancelAllApprovals), and (b) resolves all WorkerServerRequestBroker.pending continuations with WorkerServerResponse(failed: true). Add a broker method (e.g. cancelAll()) to WorkerServerRequestBroker since pending is private there.

2. To match upstream wire semantics and unblock a frontend awaiting the approval, also notify clients. Emit ServerNotification.serverRequestResolved(threadId:requestId:) (already defined, ProtocolModel/Events.swift:188) for each aborted request id so subscribed connections can tear down their approval UI. This requires the worker to know the pending request ids (it does: keys of pendingApprovals and broker.pending) and to emit a notification per id via link.sendToSupervisor(.notification(...)). The SessionSupervisor already fans notifications out to subscribers (SessionSupervisor.swift:130) and the RequestRouter forwards them to connections (RequestRouter.swift:8590-8591).

3. Optionally carry the upstream error semantics: when failing, use a JSON-RPC -32603 error with data.reason="turnTransition" so any client matching is_turn_transition_server_request_error behavior remains compatible; define a constant mirroring TURN_TRANSITION_PENDING_REQUEST_ERROR_REASON.

The minimal correctness fix is step 1 (prevents the up-to-300s hang on interrupt). Steps 2-3 achieve full wire/notification parity.

[app-server-events] turn/plan/updated plan step status uses snake_case in_progress instead of camelCase inProgress (wrong-casing)
  RUST: app-server-protocol/src/protocol/v2/turn.rs:382-389 `TurnPlanStepStatus` is `#[serde(rename_all = "camelCase")]` with variants Pending/InProgress/Completed → wire values `pending`,`inProgress`,`completed`. Used in TurnPlanStep (turn.rs:374-380).
  SWIFT: Sources/ProtocolModel/Events.swift:441-456 PlanUpdated serializes `plan: [PlanItemArg]`; PlanItemArg.status is PlanStepStatus (Events.swift:494-498) whose wire value is `inProgress = "in_progress"` (snake_case).
  DESC: The v2 turn/plan/updated notification expects camelCase step status (`inProgress`); Swift emits snake_case (`in_progress`). PlanItemArg's snake_case is correct for the update_plan tool argument shape, but the notification surface uses TurnPlanStep with camelCase, so the notification body diverges.
  FIX: In Sources/ProtocolModel/Events.swift, the plan-update notification must serialize the step status as camelCase to match upstream TurnPlanStepStatus (pending/inProgress/completed). Do NOT change PlanStepStatus/PlanItemArg themselves (their snake_case `in_progress` is correct for parsing the update_plan tool arguments). Instead, introduce a dedicated notification step type and use it in the PlanUpdated wire struct:

1) Add a camelCase enum + step struct:
   public enum TurnPlanStepStatus: String, Codable { case pending; case inProgress; case completed }
   struct TurnPlanStep: Codable { var step: String; var status: TurnPlanStepStatus }

2) Change PlanUpdated.plan (Events.swift:445) from `[PlanItemArg]` to `[TurnPlanStep]`, and in the .planUpdate case (Events.swift:296-299) map each PlanItemArg -> TurnPlanStep, translating status: .pending->.pending, .inProgress->.inProgress, .completed->.completed (mirrors upstream From<CorePlanStepStatus> at turn.rs:400-409).

3) Separately (compounding fix, recommended): change the method string at Events.swift:222 from "item/plan/updated" to "turn/plan/updated" to match common.rs:1469, since upstream has no item/plan/updated notification.

[app-server-events] turn/diff/updated notification entirely missing (missing-notification)
  RUST: app-server-protocol/src/protocol/common.rs:1468 `TurnDiffUpdated => "turn/diff/updated"`; emitted in app-server/src/bespoke_event_handling.rs:1203-1204 + handle_turn_diff (1225-1239) on EventMsg::TurnDiff. Payload turn.rs:358-362 {threadId, turnId, diff}.
  SWIFT: ABSENT. No `turn/diff/updated` / `turnDiff` case in ServerNotification (Sources/ProtocolModel/Events.swift) and no emission anywhere in Sources/ (grep across full tree returns nothing).
  DESC: Upstream streams the aggregated unified diff for the turn via turn/diff/updated whenever file changes accrue. Swift never produces this notification, so a frontend cannot render the live turn diff.
  FIX: Reproduce the upstream turn-diff pipeline in the Swift port:

1) Add the notification case. In Sources/ProtocolModel/Events.swift add to the ServerNotification enum (near .turnCompleted, line ~138):
   `case turnDiffUpdated(threadId: ThreadId, turnId: TurnId, diff: String)`
   Add to the method switch (~line 217): `case .turnDiffUpdated: return "turn/diff/updated"`
   Add params encoding in the params/encode switch (the block starting ~line 254) mirroring the upstream payload shape: `{ "threadId": tid, "turnId": turnId, "diff": diff }`.

2) Add turn-level diff aggregation. Introduce a lightweight TurnDiffTracker (e.g. Sources/Tools/TurnDiffTracker.swift) that records each applied apply_patch result and produces the aggregated unified diff for the turn. ApplyPatch already yields per-file unifiedDiff (Sources/Tools/ApplyPatch.swift), so the tracker can accumulate file path -> latest unified diff (matching upstream baseline-vs-current semantics: track the turn's starting baseline per touched file and recompute the aggregated diff after each change).

3) Emit after each file change. In Sources/HarnessCore/SessionEngine.swift, in the apply_patch handling path (around the apply_patch branch at ~line 618 and where FileChange/itemCompleted is emitted), after a successful patch apply call the tracker and `emit(.turnDiffUpdated(threadId:turnId:diff: aggregatedUnifiedDiff))`, matching upstream which fires after every FileChange item. Reset the tracker baseline at turn start.

As a minimal interim fix (if full baseline tracking is too large), at least: add the .turnDiffUpdated enum case + method/params encoding, and emit it with the concatenation of per-file unified diffs accumulated during the turn after each apply_patch, so frontends receive a turn/diff/updated stream with the correct shape.

[app-server-events] item/reasoning/textDelta missing contentIndex field (and never emitted) (wrong-field)
  RUST: app-server-protocol/src/protocol/v2/item.rs:1197-1207 ReasoningTextDeltaNotification {threadId, turnId, itemId, delta, content_index: i64}; mapped at event_mapping.rs:370-378 from EventMsg::ReasoningRawContentDelta carrying content_index.
  SWIFT: Sources/ProtocolModel/Events.swift:134 `case reasoningDelta(...delta: String)` → method `item/reasoning/textDelta` (Events.swift:213) but serialized via the shared `Delta` struct (Events.swift:373-375) {threadId, turnId, itemId, delta} — no `contentIndex`. The case is also never emitted (grep: only definition/method/encode references, no `emit(.reasoningDelta`).
  DESC: Upstream item/reasoning/textDelta carries a required `contentIndex` (i64) distinguishing concurrent reasoning content streams. Swift omits it. It is also dead code — ModelClient.ResponseEvent (Sources/ModelClient/ModelClient.swift:197-207) has no reasoning-delta variant, so the engine cannot produce it.
  FIX: Two coordinated changes (both needed for full parity; the field fix alone leaves dead code, the wiring fix alone re-introduces the missing field):

1) Carry contentIndex on the notification. In Sources/ProtocolModel/Events.swift:
   - Change the enum case (line 134) to: case reasoningDelta(threadId: ThreadId, turnId: TurnId, itemId: ItemId, delta: String, contentIndex: Int)
   - Stop routing .reasoningDelta through the shared `Delta` struct. Remove it from the combined pattern at lines 267-271 and give it its own arm that encodes a dedicated struct, e.g.:
       struct ReasoningTextDelta: Codable { var threadId: ThreadId; var turnId: TurnId; var itemId: ItemId; var delta: String; var contentIndex: Int }
     and `case .reasoningDelta(let tid, let turnId, let itemId, let delta, let ci): params = try JSONBridge.value(ReasoningTextDelta(threadId: tid, turnId: turnId, itemId: itemId, delta: delta, contentIndex: ci))`. (contentIndex is required on the wire upstream, so encode unconditionally; default to 0 to match upstream serde default.)

2) Wire the producer so it is no longer dead code. In Sources/ModelClient/ModelClient.swift add a ResponseEvent variant, e.g. case reasoningDelta(itemId: String, delta: String, contentIndex: Int); parse the SSE event "response.reasoning_text.delta" (and content_index) in Sources/ModelClient/URLSessionResponsesClient.swift (alongside the existing "response.output_text.delta" arm at line 203) and the WebSocket client; then in Sources/HarnessCore/SessionEngine.swift translate that ResponseEvent into emit(.reasoningDelta(threadId:turnId:itemId:delta:contentIndex:)) the same way agentDelta is mapped to .agentMessageDelta. If full reasoning-stream support is out of scope, at minimum apply fix (1) so the existing notification case is wire-correct.

[app-server-events] item/reasoning/summaryTextDelta and item/reasoning/summaryPartAdded notifications missing (missing-notification)
  RUST: app-server-protocol/src/protocol/common.rs:1502-1503 ReasoningSummaryTextDelta => "item/reasoning/summaryTextDelta", ReasoningSummaryPartAdded => "item/reasoning/summaryPartAdded". Payloads item.rs:1177-1195: summaryTextDelta {threadId,turnId,itemId,delta,summary_index:i64}; summaryPartAdded {threadId,turnId,itemId,summary_index:i64}. Mapped at event_mapping.rs:361-369 (ReasoningContentDelta) and 379-386 (AgentReasoningSectionBreak).
  SWIFT: ABSENT. No `summaryTextDelta` / `summaryPartAdded` case or method in Sources/ProtocolModel/Events.swift; grep across Sources/ returns nothing. Swift's single `reasoningDelta` maps only to item/reasoning/textDelta and conflates the two upstream channels.
  DESC: Upstream distinguishes raw reasoning text (item/reasoning/textDelta with contentIndex) from summarized reasoning (item/reasoning/summaryTextDelta with summaryIndex) plus a summaryPartAdded marker for new summary sections. Swift implements neither summary notification, so reasoning-summary streaming UI cannot work.
  FIX: Implement the reasoning streaming pipeline end-to-end:

1. Sources/ModelClient/ModelClient.swift (ResponseEvent enum, ~line 197): add cases
   `case reasoningSummaryDelta(itemId: String, delta: String, summaryIndex: Int)`
   `case reasoningTextDelta(itemId: String, delta: String, contentIndex: Int)`  (raw channel; optional but matches textDelta)
   `case reasoningSummaryPartAdded(itemId: String, summaryIndex: Int)`

2. Sources/ModelClient/OpenAIResponsesClient.swift SSE switch (~line 409): add
   `case "response.reasoning_summary_text.delta":` -> yield .reasoningSummaryDelta(itemId: obj["item_id"], delta: obj["delta"], summaryIndex: obj["summary_index"])
   `case "response.reasoning_text.delta":` -> yield .reasoningTextDelta(...)
   `case "response.reasoning_summary_part.added":` -> yield .reasoningSummaryPartAdded(itemId, summaryIndex: obj["summary_index"])
   (extract summary_index/content_index as Int with default 0).

3. Sources/ProtocolModel/Events.swift: replace/extend the dead `reasoningDelta` case with three cases mirroring upstream and add their method strings + payload arms:
   `case reasoningSummaryDelta(threadId,turnId,itemId,delta,summaryIndex:Int)` -> "item/reasoning/summaryTextDelta"
   `case reasoningTextDelta(threadId,turnId,itemId,delta,contentIndex:Int)` -> "item/reasoning/textDelta"
   `case reasoningSummaryPartAdded(threadId,turnId,itemId,summaryIndex:Int)` -> "item/reasoning/summaryPartAdded"
   Add Codable payload structs with camelCase keys threadId/turnId/itemId/delta/summaryIndex (and contentIndex) in the toMessage() switch.

4. Sources/HarnessCore/SessionEngine.swift: in the ResponseEvent consumption loop (alongside the .agentDelta -> emit(.agentMessageDelta) sites ~916/1298/1679), add handling for the new ResponseEvent reasoning cases that calls emit(.reasoningSummaryDelta(...)), emit(.reasoningTextDelta(...)), emit(.reasoningSummaryPartAdded(...)) using config.threadId/turnId and the streamed itemId/summaryIndex.

[app-server-events] item/commandExecution/terminalInteraction notification missing (missing-notification)
  RUST: app-server-protocol/src/protocol/common.rs:1488 `TerminalInteraction => "item/commandExecution/terminalInteraction"`; payload item.rs:1209-1218 {threadId,turnId,itemId,process_id,stdin}; mapped event_mapping.rs:431-439 and dispatched bespoke_event_handling.rs:971-980 (EventMsg::TerminalInteraction).
  SWIFT: ABSENT. No terminalInteraction case/method anywhere in Sources/ (grep returns nothing).
  DESC: Upstream emits item/commandExecution/terminalInteraction to surface PTY stdin echo/interaction for unified-exec sessions. Swift has no such notification, so terminal-interaction UI cannot be driven.
  FIX: Add a TerminalInteraction notification mirroring the existing commandOutputDelta path. (1) In Sources/ProtocolModel/Events.swift: add case to ServerNotification enum near line 135: `case terminalInteraction(threadId: ThreadId, turnId: TurnId, itemId: ItemId, processId: String, stdin: String)`; add method string near line 214: `case .terminalInteraction: return "item/commandExecution/terminalInteraction"`; and add an encoding arm in the params switch (near the .commandOutputDelta arm ~line 269) emitting a camelCase object {threadId, turnId, itemId, processId, stdin}. (2) Add a CodableEvent payload analog of EventMsg::TerminalInteraction (call_id/process_id/stdin) and emit it from the write_stdin execution. Concretely, in Sources/HarnessCore/SessionEngine.swift where write_stdin is run, after a successful UnifiedExecManager.writeStdin, yield onto the existing eventCont sink: `.terminalInteraction(threadId: config.threadId, turnId: turnId, itemId: ItemId(callId), processId: String(sessionId), stdin: charsWritten)` — using the same threadId/turnId/itemId values already captured for commandOutputDelta (lines 1326-1339). Use the raw model-provided chars (NOT the EOT-appended `toWrite`) for the stdin field to match upstream which sends args.chars. Note the itemId should be the exec session's item id (the upstream call_id from exec_command), so thread the originating exec call_id through rather than the write_stdin call's own callId if they differ. RequestRouter already forwards ServerNotification.toMessage() to the client, so no router change is needed.

[session-turn-loop] Missing `turn/diff/updated` notification (turn-level aggregated unified diff) — TurnDiffTracker not reproduced (missing-notification)
  RUST: turn_diff_tracker.rs:18-107 maintains a per-turn TurnDiffTracker and `get_unified_diff()`. session/turn.rs:2306-2314 emits `EventMsg::TurnDiff(TurnDiffEvent{unified_diff})` after each turn step when patches were applied; app-server/src/bespoke_event_handling.rs:1231-1238 maps it to `ServerNotification::TurnDiffUpdated(TurnDiffUpdatedNotification{thread_id,turn_id,diff})`. Method string is `turn/diff/updated` (app-server-protocol/src/protocol/common.rs:1468) with payload `{threadId,turnId,diff}` (v2/turn.rs:353-362).
  SWIFT: ABSENT. Sources/ProtocolModel/Events.swift ServerNotification enum (lines 125-201) has no diff case and the method switch (203-236) has no "turn/diff/updated". grep across Sources/ for diff/updated|TurnDiffTracker|getUnifiedDiff|trackDelta returns only per-patch helpers in Sources/Tools/ApplyPatch.swift (unifiedDiff per change); no per-turn aggregation and no notification emission anywhere in SessionEngine.swift.
  DESC: Upstream tracks the net text diff for the whole turn from committed apply_patch mutations (without re-reading the filesystem) and pushes a cumulative `turn/diff/updated` notification so the frontend can render the running aggregate turn diff. The Swift port only computes per-patch unified diffs inside the apply_patch tool and never aggregates or emits the turn-level diff notification, so the frontend's turn-diff view will never update.
  FIX: 1) Add a tracker type, e.g. Sources/HarnessCore/TurnDiffTracker.swift, porting turn_diff_tracker.rs: maintain `baselineByPath: [String:String]`, `currentByPath: [String:String]`, `originByCurrentPath`, and a `valid` flag; methods `trackDelta(addedPaths/updatedPaths/deletedPaths + old/new content)`, `invalidate()`, and `unifiedDiff() -> String?` that aggregates per-path unified diffs (reuse ApplyPatch.makeUnifiedDiff) sorted by display path, with rename pairing. Non-exact patches call invalidate().

2) In Sources/ProtocolModel/Events.swift: add `case turnDiffUpdated(threadId: ThreadId, turnId: TurnId, diff: String)` to ServerNotification; in the `method` switch return "turn/diff/updated"; in `toMessage()` encode params `{threadId, turnId, diff}` (camelCase) to match TurnDiffUpdatedNotification.

3) In Sources/HarnessCore/SessionEngine.swift: instantiate a TurnDiffTracker per turn (reset at turn start). In the apply_patch success path (around the `sandboxed()` result near lines 618-645, after a patch commits), capture `previous = tracker.unifiedDiff()`, feed the applied changes via `tracker.trackDelta(...)` (or `invalidate()` for non-exact patches), compute `current = tracker.unifiedDiff()`, and if `current != previous && current != nil` call `emit(.turnDiffUpdated(threadId: config.threadId, turnId: turnId, diff: current!))`. This matches upstream tools/events.rs gating (emit only when the aggregate changes). The applied-change content needed to feed trackDelta is already produced by ApplyPatch (old/new contents per change).

[session-turn-loop] Plan-update notification uses wrong method name `item/plan/updated` (upstream: `turn/plan/updated`) (wrong-casing)
  RUST: app-server-protocol/src/protocol/common.rs:1469 `TurnPlanUpdated => "turn/plan/updated" (v2::TurnPlanUpdatedNotification)`; payload v2/turn.rs:364-372 `TurnPlanUpdatedNotification{thread_id,turn_id,explanation:Option<String>,plan:Vec<TurnPlanStep>}`. There is no `item/plan/updated` method upstream (only `item/plan/delta` for streaming deltas, common.rs:1478).
  SWIFT: Sources/ProtocolModel/Events.swift:222 `case .planUpdate: return "item/plan/updated"`; emitted by the update_plan tool path (Sources/Tools/UpdatePlan.swift:39, InfraPrimitives/PlanUpdateBus.swift:10) as `.planUpdate`.
  DESC: The update_plan tool's notification is published under the method `item/plan/updated`, but the upstream app-server v2 method is `turn/plan/updated`. A frontend listening on `turn/plan/updated` will never receive plan updates from the Swift server.
  FIX: 1) In Sources/ProtocolModel/Events.swift:222 change `case .planUpdate: return "item/plan/updated"` to `case .planUpdate: return "turn/plan/updated"`. 2) Update the two doc comments that reference the wrong name (Events.swift:439 and the UpdatePlan.swift / Events comments) for accuracy. 3) Update the test assertion in Tests/ProtocolModelTests/ProtocolModelTests.swift:800 from `XCTAssertEqual(msg.method, "item/plan/updated")` to `"turn/plan/updated"`. No payload changes are needed — the PlanUpdated struct already matches upstream TurnPlanUpdatedNotification.

[session-turn-loop] Plan step status serialized as `in_progress` (snake_case) in the v2 plan notification; upstream v2 uses camelCase `inProgress` (wrong-casing)
  RUST: app-server-protocol/src/protocol/v2/turn.rs:382-389 `#[serde(rename_all = "camelCase")] enum TurnPlanStepStatus { Pending, InProgress, Completed }` => wire values pending/inProgress/completed; TurnPlanStep{step,status} (v2/turn.rs:374-380) is the element type of TurnPlanUpdatedNotification.plan.
  SWIFT: Sources/ProtocolModel/Events.swift:494-498 `enum PlanStepStatus { case pending; case inProgress = "in_progress"; case completed }` is used directly as the `status` of PlanItemArg (Events.swift:502-508), which is what `.planUpdate`/PlanUpdated (Events.swift:296-299, 441-456) serializes. So the wire value is `in_progress`.
  DESC: The Swift port reuses the core `plan_tool` enum (snake_case `in_progress`) as the status field of the v2 plan-update notification, but the app-server v2 `TurnPlanStepStatus` is camelCase (`inProgress`). The notification therefore emits `"status":"in_progress"` where the frontend expects `"inProgress"`, so an in-progress plan step will not be recognized.
  FIX: The notification surface must use a camelCase status distinct from the snake_case tool-input enum, mirroring upstream's `StepStatus` (input) vs `TurnPlanStepStatus` (notification) split.

Concrete change in Sources/ProtocolModel/Events.swift:
- Keep `PlanStepStatus` (snake_case, rawValues pending/in_progress/completed) for decoding `update_plan` tool arguments only.
- Introduce a v2 notification enum, e.g.:
  `public enum TurnPlanStepStatus: String, Sendable, Codable, Equatable { case pending; case inProgress; case completed }`
  (default Swift String rawValues already give "pending"/"inProgress"/"completed").
- Change `PlanItemArg.status` used by the `PlanUpdated` wire struct to `TurnPlanStepStatus` (or add a dedicated wire struct `TurnPlanStep { step; status: TurnPlanStepStatus }` and have `PlanUpdated.plan` be `[TurnPlanStep]`), converting from the parsed snake_case input enum at construction time (pending->pending, in_progress->inProgress, completed->completed) exactly as upstream `TurnPlanStepStatus::from`.
- Wherever the `.planUpdate` event is constructed from the PlanUpdateBus payload (parse with the snake_case `PlanStepStatus`, then map into `TurnPlanStepStatus` before building `PlanUpdated`).

This yields wire output `"status":"inProgress"` matching upstream. (Separately, also correct the method string at Events.swift:222 from "item/plan/updated" to "turn/plan/updated" to match upstream common.rs:1469.)

[context-compaction] Auto-compact threshold uses effective-context-percent (95%) instead of the hardcoded 90% window factor (wrong-semantics)
  RUST: protocol/src/openai_models.rs:322-333 auto_compact_token_limit(): `let context_limit = self.resolved_context_window().map(|cw| (cw * 9) / 10); ... config_limit.map_or(context_limit, |limit| std::cmp::min(limit, context_limit))`. The factor is a fixed 9/10 (90%) and is NOT effective_context_window_percent (which defaults to 95 at openai_models.rs:252-254 and is used for input budgeting, not the compaction limit). turn.rs:154/744-749 then compares total_usage_tokens >= this limit.
  SWIFT: Sources/Tokenizer/ModelCatalog.swift:38-40 `autoCompactTokenLimit { max(1, contextWindow * effectiveContextPercent / 100) }` with effectiveContextPercent defaulting to 95 (line 11/20, no per-entry override). codex-session/main.swift:266 and codexd/main.swift:249 feed this into SessionEngine.autoCompactTokens; SessionEngine.swift:1071 & 1487 compare `ctx.totalTokenUsage() >= autoCompactTokens`. Net effect: 95% of window instead of upstream 90% (e.g. 272k window → Swift 258,400 vs upstream 244,800).
  DESC: Upstream derives the auto-compact limit as min(config_limit, 90% of context window) using a dedicated hardcoded 9/10 factor. Swift instead multiplies the context window by effective_context_window_percent (95%), conflating two distinct upstream concepts, and never applies the config-limit min. As a result Swift fires auto-compaction (both pre-turn at SessionEngine.swift:1071 and mid-turn at :1487) ~5% of the context window later than upstream, and ignores any explicit model_auto_compact_token_limit cap.
  FIX: In /Users/chabotc/Projects/codex-swift/Sources/Tokenizer/ModelCatalog.swift, change the autoCompactTokenLimit computed property (lines 38-40) to use the hardcoded 90% factor matching upstream's (context_window * 9) / 10, and stop using effectiveContextPercent for this:

    /// codex `auto_compact_token_limit` derivation: floor(context_window * 9 / 10).
    /// Note: this is a fixed 90% factor, distinct from effective_context_window_percent.
    public var autoCompactTokenLimit: Int {
        max(1, contextWindow * 9 / 10)
    }

This yields 244,800 for a 272k window, matching upstream. effectiveContextPercent remains for its real purpose (input budgeting) and should not be removed. Optionally, to also reproduce the config-override clamp, add an optional `autoCompactTokenLimitOverride: Int?` field to ModelEntry and return min(override, contextWindow * 9 / 10) when present (parsing models.json's auto_compact_token_limit), but this is secondary since all current entries are null.

[context-compaction] Remote compaction (the /compact endpoint) is not implemented; default OpenAI provider always falls back to inline summarization (missing-method)
  RUST: compact.rs:65-67 `should_use_remote_compact_task(provider) = provider.supports_remote_compaction()`; model-provider-info/src/lib.rs:393-395 `supports_remote_compaction() = self.is_openai() || is_azure_responses_provider(...)`. turn.rs:823-853 run_auto_compact: when remote is supported it calls run_inline_remote_auto_compact_task[_v2] (compact_remote.rs / compact_remote_v2.rs) which POSTs the history to the compact endpoint (compact_remote.rs:192-222 compact_conversation_history) and post-processes via process_compacted_history/should_keep_compacted_history_item; only non-OpenAI providers fall to the inline run_inline_auto_compact_task.
  SWIFT: Sources/ModelClient/ModelProvider.swift:80-82 default provider is OpenAI (https://api.openai.com/v1). No remote-compaction code exists in Sources/ (grep for remote.*compact / compactConversation finds only the literal feature-name string 'remote_compaction_v2' in RequestRouter.swift:3436, a passthrough flag list). SessionEngine.runCompactionFlow (SessionEngine.swift:864) always performs the inline model-call summarization (compact.rs path).
  DESC: For the production provider (OpenAI), upstream compacts by calling the dedicated remote /compact endpoint and installing the model-returned transcript (filtered by should_keep_compacted_history_item, with history trimmed to fit via trim_function_call_history_to_fit_context_window). The Swift port has no equivalent and always synthesizes the inline summary+recent-user-messages history. The resulting post-compaction history therefore differs from production codex for the same conversation, which a frontend that diffs/inspects compacted history would observe.
  FIX: 1. Add provider capability: in Sources/ModelClient/ModelProvider.swift add `var supportsRemoteCompaction: Bool { id == "openai" || isAzureResponsesProvider }` mirroring model-provider-info/src/lib.rs:393-395 (is_openai() || is_azure_responses_provider(name, baseURL)).
2. Add a remote-compaction client method on the responses client (OpenAIResponsesClient.swift / URLSessionResponsesClient.swift): `compactConversationHistory(prompt:modelInfo:settings:) async throws -> [ResponseItem]` that POSTs to the "/responses/compact" endpoint (mirror client.rs:433-470 build_responses_request against RESPONSES_COMPACT_ENDPOINT, service_tier=nil when ApiKey auth) and decodes the returned ResponseItem array.
3. Port the post-processing helpers from compact_remote.rs into Sources/HarnessCore/Compaction.swift: `shouldKeepCompactedHistoryItem(_:)` (compact_remote.rs:293), `processCompactedHistory(...)` (compact_remote.rs:255, applies retain + initial-context injection), and `trimFunctionCallHistoryToFitContextWindow(...)` (compact_remote.rs:361).
4. Branch in SessionEngine.runCompactionFlow (SessionEngine.swift:864): if `provider.supportsRemoteCompaction`, build the prompt, call model.compactConversationHistory, run trimFunctionCallHistoryToFitContextWindow then processCompactedHistory, and `ctx.replace(newHistory)` — instead of the inline stream-and-summarize path. Keep the existing inline path only for non-OpenAI providers. Gate the v2 variant on the RemoteCompactionV2 feature flag (already passed through in RequestRouter.swift:3436) to match turn.rs:824-834.

[model-client] Reasoning SSE events are dropped entirely (no reasoning summary/content delta surfaced) (missing-notification)
  RUST: codex-api/src/sse/responses.rs:291-306 maps "response.reasoning_summary_text.delta" -> ResponseEvent::ReasoningSummaryDelta{delta,summary_index}, "response.reasoning_text.delta" -> ResponseEvent::ReasoningContentDelta{delta,content_index}; lines 384-390 map "response.reasoning_summary_part.added" -> ResponseEvent::ReasoningSummaryPartAdded{summary_index}. common.rs:98-108 declares these ResponseEvent variants.
  SWIFT: ABSENT. Sources/ModelClient/ModelClient.swift:197-207 ResponseEvent has only .created/.agentDelta/.agentDone/.toolCall/.completed. All three SSE parsers fall through to `default: continue/break` for reasoning event types: OpenAIResponsesClient.swift:506-507, URLSessionResponsesClient.swift:268-269, WebSocketResponsesClient.swift:255-256. grep for reasoning_summary_text/reasoning_text/reasoning_summary_part across Sources/ returns nothing.
  DESC: Upstream emits dedicated reasoning-stream events that the agent renders as live thought summaries and reasoning content. Swift requests reasoning (sends reasoning object + include:[reasoning.encrypted_content]) but then discards every reasoning.* SSE event, so a frontend can never display reasoning summaries or content for reasoning-capable models.
  FIX: Wire reasoning SSE through to the existing (currently dead) ServerNotification.reasoningDelta. Concretely: (1) In Sources/ModelClient/ModelClient.swift add ResponseEvent cases: `case reasoningSummaryDelta(delta: String, summaryIndex: Int)`, `case reasoningContentDelta(delta: String, contentIndex: Int)`, `case reasoningSummaryPartAdded(summaryIndex: Int)`. (2) In all three parsers add handling before the default arm: OpenAIResponsesClient.swift (~line 506), URLSessionResponsesClient.swift (~line 268), WebSocketResponsesClient.swift (~line 255) — for kind "response.reasoning_summary_text.delta" read obj[\"delta\"] + obj[\"summary_index\"] and yield .reasoningSummaryDelta; "response.reasoning_text.delta" -> obj[\"delta\"] + obj[\"content_index\"] -> .reasoningContentDelta; "response.reasoning_summary_part.added" -> obj[\"summary_index\"] -> .reasoningSummaryPartAdded. (3) In Sources/HarnessCore/SessionEngine.swift, where ResponseEvent is translated into Events for the active item, map .reasoningSummaryDelta and .reasoningContentDelta to ServerNotification.reasoningDelta(threadId, turnId, itemId: activeReasoningItemId, delta:) (mirroring turn.rs which gates on an active streaming item). Optionally add a section-break Event mirroring upstream AgentReasoningSectionBreak for .reasoningSummaryPartAdded; if added, extend Events.swift with the variant and a wire string. Minimum viable parity is steps 1-3 so reasoning summary/content deltas actually reach frontends via the already-declared reasoningDelta notification.

[model-client] response.output_item.added is not handled (no live item-added event) (missing-notification)
  RUST: codex-api/src/sse/responses.rs:376-383 maps "response.output_item.added" -> ResponseEvent::OutputItemAdded(item); common.rs:75 declares OutputItemAdded(ResponseItem).
  SWIFT: ABSENT. None of the three parsers handle "response.output_item.added"; only "response.output_item.done" is matched (OpenAIResponsesClient.swift:419, URLSessionResponsesClient.swift:207, WebSocketResponsesClient.swift:190). No OutputItemAdded analog in ResponseEvent (ModelClient.swift:197-207).
  DESC: Upstream surfaces output_item.added so the UI can render an item (message/tool call) as soon as it begins, before it is finalized. Swift only consumes the terminal .done, losing the live-add signal.
  FIX: 1) Add an enum case in Sources/ModelClient/ModelClient.swift ResponseEvent (after .toolCall): `case outputItemAdded(item: ResponseItemJSON)` — or, to match the existing decode style, `case itemStarted(itemId: String, kind: String, text: String?)` carrying the begun item's id/type and any pre-populated text.
2) In each of the three parsers, add a `case "response.output_item.added":` arm mirroring the existing `response.output_item.done` arm: parse obj["item"], and yield the new event. For a function_call/custom_tool_call item, surface call_id/name so the engine can set up the (currently nonexistent) tool-argument diff path; for a message item, surface id and any seeded content text.
   - OpenAIResponsesClient.swift: add the arm next to line 419.
   - URLSessionResponsesClient.swift: add the arm next to line 207.
   - WebSocketResponsesClient.swift: add the arm next to line 190.
3) In Sources/HarnessCore/SessionEngine.swift, handle the new case in the main stream consumer (switch at line 1294): emit `.itemStarted` for non-tool items (assistant message / reasoning) at the .added boundary so the UI renders them as they begin, and seed any pre-populated assistant text into the delta/parser path. For tool-call items, move/duplicate the existing synchronous `.itemStarted` emission (currently keyed off .toolCall at line 1318) to fire on .added so the in-progress signal matches upstream timing.
   Minimal-risk variant: just decode and emit the .added event and forward `.itemStarted` for non-tool items; the full tool-argument diff-consumer streaming (upstream create_diff_consumer) can be deferred since the port has no diff-consumer machinery yet — but the item-started notification at the .added boundary should be reproduced.

[model-client] Incremental tool-call input deltas (custom_tool_call_input.delta / function_call_arguments.delta) dropped (missing-notification)
  RUST: codex-api/src/sse/responses.rs:280-289 maps "response.custom_tool_call_input.delta" -> ResponseEvent::ToolCallInputDelta{item_id,call_id,delta}; client_common_tests/sse tests (responses.rs:766-795) show "response.function_call_arguments.delta" is also surfaced as ToolCallInputDelta via the item_id fallback. common.rs:93-97 declares ToolCallInputDelta.
  SWIFT: ABSENT. No handling of "response.custom_tool_call_input.delta" or "response.function_call_arguments.delta" in any parser; they hit the default branch. ResponseEvent has no ToolCallInputDelta analog (ModelClient.swift:197-207).
  DESC: Upstream streams partial tool-call arguments (e.g. apply_patch input, function arguments) as deltas so the UI can show tool calls forming. Swift only delivers the final arguments via output_item.done .toolCall, dropping the incremental stream entirely.
  FIX: 1) Add a case to ResponseEvent in Sources/ModelClient/ModelClient.swift (after .agentDelta): `case toolCallInputDelta(itemId: String, callId: String?, delta: String)`.
2) In each of the three SSE parsers, before the `default` branch, add handling for both upstream event types, mirroring the Rust item_id-or-call_id fallback:
   - OpenAIResponsesClient.swift (~line 505), URLSessionResponsesClient.swift (~line 248), WebSocketResponsesClient.swift (~line 233):
     ```swift
     case "response.custom_tool_call_input.delta", "response.function_call_arguments.delta":
         let delta = obj["delta"] as? String ?? ""
         let callId = obj["call_id"] as? String
         let itemId = (obj["item_id"] as? String) ?? callId
         if !delta.isEmpty, let itemId = itemId {
             cont.yield(.toolCallInputDelta(itemId: itemId, callId: callId, delta: delta))
         }
     ```
3) Handle the new event in the turn/stream consumer (SessionEngine.swift, alongside where .agentDelta/.reasoningDelta are forwarded) so it emits an item/tool-call argument-delta event analogous to upstream's tool_argument_diff_consumer. If the Swift port has no tool-call-forming UI event type, add one to Sources/ProtocolModel/Events.swift mirroring how reasoningDelta is modeled. (If the project intentionally only surfaces final tool calls, the minimal fix is steps 1-2 plus a no-op/buffer consumer; but to match upstream fidelity the UI event must be emitted.)

[model-client] Server-effective model (OpenAI-Model header / response headers) is never surfaced (missing-notification)
  RUST: codex-api/src/sse/responses.rs:25 OPENAI_MODEL_HEADER="openai-model"; lines 41-45 + 65-67 emit ResponseEvent::ServerModel from the response header; ResponsesStreamEvent::response_model() (lines 170-184) + process_sse (448-459) also emit ServerModel from per-event response.headers/top-level headers (used when backend safety routing swaps the model). common.rs:76-79.
  SWIFT: ABSENT. URLSessionResponsesClient.swift:124-147 reads only rate-limit + Retry-After headers; never reads openai-model. No ResponseEvent.serverModel analog. grep openai-model/ServerModel across Sources/ returns nothing.
  DESC: When backend safety routing serves a different model than requested, upstream notifies the client (ServerModel) so the UI reflects the actual model. Swift never reads the header or the per-event response.headers, so the frontend cannot detect a routed model swap.
  FIX: Wire the openai-model header through to the existing (currently-dead) modelRerouted event:

1. Sources/ModelClient/ModelClient.swift: add a case to ResponseEvent, e.g. `case serverModel(String)` (keep Equatable/Sendable conformance).

2. Sources/ModelClient/URLSessionResponsesClient.swift (readSSE, in the `if let http = response as? HTTPURLResponse` block around lines 124-147, before the SSE loop): read the header and emit it first, e.g.
   `if let model = http.value(forHTTPHeaderField: "openai-model") ?? http.value(forHTTPHeaderField: "x-openai-model") { cont.yield(.serverModel(model)) }`
   (Use allHeaderFields case-insensitively like the existing rateLimits/retryAfter helpers do, since HTTP header lookups should be case-insensitive.) Add the equivalent header read in WebSocketResponsesClient.swift and OpenAIResponsesClient.swift for parity. Optionally add the per-event header path (per-event response.headers / top-level "openai-model"/"x-openai-model") to mirror responses.rs:170-210/448-459 for mid-stream reroutes.

3. Sources/HarnessCore/SessionEngine.swift (in each turn loop that switches over rs.events, primarily ~1286): add `case .serverModel(let model):` that compares `model.lowercased()` against the requested model slug (from the turn/model context). On mismatch (once per turn, guarded by a flag mirroring server_model_warning_emitted), emit the existing `.modelRerouted(threadId:turnId:from:to:reason:)` thread event (reason "high_risk_cyber_activity") plus a user-facing warning string, matching session/mod.rs:2520-2557. On match, just log/ignore.

This reuses the already-defined Events.swift `modelRerouted` case and makes it actually fire.

[tools-router] apply_patch exposed as JSON function tool instead of upstream custom/freeform lark-grammar tool (wrong-semantics)
  RUST: core/src/tools/handlers/apply_patch_spec.rs:18-26 create_apply_patch_freeform_tool returns ToolSpec::Freeform(FreeformTool{ name:"apply_patch", description:"Use the `apply_patch` tool to edit files. This is a FREEFORM tool, so do not wrap the patch in JSON.", format: FreeformToolFormat{ type:"grammar", syntax:"lark", definition: APPLY_PATCH_LARK_GRAMMAR } }); apply_patch.rs:306-307 fn spec(&self) -> Option<ToolSpec> { Some(create_apply_patch_freeform_tool(...)) } (always freeform). tool_spec.rs:49-51 serializes Freeform with #[serde(rename="custom")].
  SWIFT: Sources/Tools/ToolRouter.swift:381-389 ApplyPatchTool name="apply_patch", description "Apply a Codex apply_patch envelope to the workspace...", jsonSchema is a normal object {"patch":{"type":"string"...}}. Sources/ModelClient/OpenAIResponsesClient.swift:191 serializes it as {"type":"function",...}. Swift response parser only handles function_call items (OpenAIResponsesClient.swift:434), not custom_tool_call.
  DESC: Upstream always advertises apply_patch as a Responses-API custom tool (type:"custom") carrying a lark grammar, yielding custom_tool_call responses where the raw patch text is sent un-JSON-wrapped. Swift advertises it as a type:"function" tool with a JSON {patch:string} arg and only accepts function_call responses. A model running the upstream system prompt (which instructs freeform un-wrapped patches) will not be driven correctly, and the wire type sent to OpenAI differs.
  FIX: 1) Add a freeform variant to the tool spec model. In Sources/ModelClient/ModelClient.swift extend ToolSpec with an optional freeform descriptor, e.g. `public var freeform: FreeformToolFormat?` where FreeformToolFormat { let type = "grammar"; let syntax = "lark"; let definition: String }. 2) Teach the Tool protocol/ApplyPatchTool to advertise it: ToolRouter.specs() should propagate the freeform descriptor (add `var freeformFormat: FreeformToolFormat? { nil }` to the Tool protocol, override in ApplyPatchTool to return the lark grammar; also change ApplyPatchTool.toolDescription to "Use the `apply_patch` tool to edit files. This is a FREEFORM tool, so do not wrap the patch in JSON." to match apply_patch_spec.rs). Bundle the lark grammar (port core/src/tools/handlers/apply_patch.lark) into Sources/Tools/Resources. 3) In OpenAIResponsesClient.swift:187-204 (and the URLSession/WebSocket request builders if they build tool arrays) branch: when spec.freeform != nil emit {"type":"custom","name":spec.name,"description":spec.description,"format":{"type":"grammar","syntax":"lark","definition":...}} instead of the function entry. 4) In all three response parsers (OpenAIResponsesClient.swift:434, URLSessionResponsesClient.swift:219, WebSocketResponsesClient.swift:202) add a `custom_tool_call` case that yields a toolCall whose argumentsJSON wraps the raw `input` text as {"patch": <raw text>} (so ApplyPatchTool.run, which decodes {patch:String}, still works) — and emit the corresponding custom_tool_call_output rather than function_call_output when sending the result back (OpenAIResponsesClient.swift:144-146 currently always emits function_call/function_call_output). 5) Gate inclusion behind a model-info/config flag mirroring upstream apply_patch_tool_type so apply_patch is only advertised when the active model expects the freeform tool.

[tools-router] shell_command schema uses cwd/timeoutMs instead of upstream workdir/timeout_ms (and decoder drops workdir) (wrong-field)
  RUST: core/src/tools/handlers/shell_spec.rs:147-215 create_shell_command_tool properties are "command", "workdir" ("The working directory to execute the command in"), "timeout_ms" ("The timeout for the command in milliseconds"), optional "login", plus approval params; description states 'Always set the `workdir` param ... Do not use `cd`'.
  SWIFT: Sources/Tools/ShellTool.swift:182-183 jsonSchema advertises {"command","cwd","timeoutMs"}; ShellTool.swift:213 CodingKeys = command, cwd, timeoutMs, timeout_ms (no workdir). So workdir sent by the model is parsed into nothing and the command runs in the default cwd.
  DESC: The model-facing shell tool parameter names diverge from upstream: cwd vs workdir and timeoutMs vs timeout_ms. Because the upstream system prompt and the tool description both tell the model to set `workdir`, a model emitting workdir against the Swift tool has its working-directory argument silently ignored, running the command in the wrong directory. login and approval params are also absent.
  FIX: In Sources/Tools/ShellTool.swift: (1) change jsonSchema (line 183) to advertise upstream names — properties "command", "workdir" {"type":"string","description":"The working directory to execute the command in"}, "timeout_ms" {"type":"number","description":"The timeout for the command in milliseconds"} (keep cwd/timeoutMs as accepted aliases if backward-compat is desired, but advertise workdir/timeout_ms to match upstream). (2) In struct Args add `workdir` and `timeout` to CodingKeys and decode cwd = decodeIfPresent(.workdir) ?? decodeIfPresent(.cwd); timeoutMs = decodeIfPresent(.timeout_ms) ?? decodeIfPresent(.timeoutMs) ?? decodeIfPresent(.timeout). (3) Update toolDescription to include the upstream non-Windows guidance "Always set the `workdir` param when using the shell_command function. Do not use `cd` unless absolutely necessary." Apply the identical fix to RemoteExecServerShellTool in Sources/Tools/RemoteExecServerTools.swift:301-330 (jsonSchema line 307 and Args CodingKeys line 322). Optionally add login/approval params for full schema parity, but the workdir alias is the load-bearing fix.

[exec-unified-shell] exec_command / write_stdin emit JSON envelope but upstream returns a plain-text section format to the model (wrong-semantics)
  RUST: core/src/tools/context.rs:330-339 `to_response_item` returns `response_text()` (the normal, non-code-mode path used by registry.rs:120). response_text() (context.rs:400-426) builds plain text sections: optional `Chunk ID: {id}`, `Wall time: {:.4} seconds`, optional `Process exited with code {code}`, optional `Process running with session ID {id}`, optional `Original token count: {n}`, then `Output:` + truncated output. The JSON form (code_mode_result, context.rs:363-390) is only used in code-mode.
  SWIFT: Sources/Tools/UnifiedExec.swift:784-809 renderExecJSON() always emits a JSON object {wall_time_seconds, output, original_token_count, optional session_id, optional exit_code}; used by ExecCommandTool (UnifiedExec.swift:945) and WriteStdinTool (UnifiedExec.swift:1050) for the model-facing ToolResult.output.
  DESC: For non-code-mode tool calls (the normal flow), upstream returns the multi-line plain-text format from response_text(), not JSON. The Swift tools always return the code-mode-style JSON, so the text the model receives diverges from upstream in both shape and content (e.g. upstream prints `Process running with session ID N` / `Process exited with code N`, the wall-time header `Wall time: X.XXXX seconds`).
  FIX: In Sources/Tools/UnifiedExec.swift, replace `renderExecJSON` with a function that reproduces upstream `response_text()` for the DIRECT tool path, since ExecCommandTool/WriteStdinTool are registered as direct tools (ShellTool.swift:722-726). Build a newline-joined section string:

private func renderExecText(chunkId: String?, sessionId: Int32?, output: String, exited: Bool, exitCode: Int32?, wallTimeSeconds: Double, originalTokenCount: Int?) -> String {
    var sections: [String] = []
    if let id = chunkId, !id.isEmpty { sections.append("Chunk ID: \(id)") }
    sections.append(String(format: "Wall time: %.4f seconds", wallTimeSeconds))
    if exited, let code = exitCode { sections.append("Process exited with code \(code)") }
    if !exited, let sid = sessionId { sections.append("Process running with session ID \(sid)") }
    if let n = originalTokenCount { sections.append("Original token count: \(n)") }
    sections.append("Output:")
    sections.append(output)
    return sections.joined(separator: "\n")
}

Match upstream gating exactly:
- `Wall time` always present, formatted with 4 decimals (`%.4f`).
- `Process exited with code` only when the process finished (exit_code present); `Process running with session ID` only when still running.
- `Original token count` only when the truncation/token count is populated (upstream sets it Some(approx_token_count) on every normal success, so compute approxTokens like the current code and pass it).
- `chunk_id` should be carried through from the UnifiedExec result if available; if the Swift manager does not currently surface a chunk id, generate one (upstream always sets it via generate_chunk_id) or omit it (matching the empty-chunk_id test cases) — but the wall-time/output/session/exit lines are the load-bearing parity items.

Then update ExecCommandTool.run (UnifiedExec.swift:945) and WriteStdinTool.run (UnifiedExec.swift:1050) to call renderExecText instead of renderExecJSON, and update the tool descriptions/jsonSchema prose (UnifiedExec.swift:821-831, 971-978) that currently advertise "structured JSON object" so the model is told to parse the plain-text sections. Keep the JSON shape only if/when these tools are dispatched as nested code-mode tools (mirroring upstream's code_mode_result vs to_response_item split); since Swift code-mode uses a verbatim callTool bridge, the simplest faithful change is plain text for the direct path. Also align the abort-path string (ToolRouter.swift:345) which already uses the plain-text `Wall time:` form.

[exec-unified-shell] exec_command tool default yield_time_ms is 250ms in Swift vs 10000ms upstream (wrong-default)
  RUST: core/src/tools/handlers/unified_exec.rs:39 `#[serde(default = "default_exec_yield_time_ms")] tty/yield...` and unified_exec.rs:63-65 `fn default_exec_yield_time_ms() -> u64 { 10_000 }` for ExecCommandArgs.yield_time_ms.
  SWIFT: Sources/Tools/UnifiedExec.swift:905 `let requested = args.yieldTimeMs ?? args.timeoutMs ?? YIELD_MIN_MS` where YIELD_MIN_MS = 250 (UnifiedExec.swift:66).
  DESC: When the model omits yield_time_ms on exec_command, upstream waits up to 10s for output before yielding; Swift waits only 250ms. This materially changes how much output is captured in the first window for non-interactive commands, frequently returning empty output where upstream would return the command result.
  FIX: In Sources/Tools/UnifiedExec.swift, add a constant matching upstream's default_exec_yield_time_ms, e.g. near line 66: `private let DEFAULT_EXEC_YIELD_TIME_MS = 10_000`. Then change ExecCommandTool's run (line 905) from `let requested = args.yieldTimeMs ?? args.timeoutMs ?? YIELD_MIN_MS` to `let requested = args.yieldTimeMs ?? args.timeoutMs ?? DEFAULT_EXEC_YIELD_TIME_MS`. The existing clamp at line 906 (`uxClamp(requested, YIELD_MIN_MS, YIELD_MAX_MS)`) already matches upstream's clamp_yield_time([250,30000]), so the 10000 default will pass through unchanged. Note: for full fidelity the sibling UnifiedExecTool open-session path (line 680, `let reqYield = args.yieldMs ?? YIELD_MIN_MS`) should arguably get the same 10000 default since it also opens new processes mapping to upstream ExecCommandArgs; the write_stdin/continue paths (lines 1022, 680's process_id branch) correctly retain the 250 default matching upstream default_write_stdin_yield_time_ms=250. The claim targets line 905 specifically, which is the precise fix needed.

[sandbox-safety-policy] Malformed exec-policy file fails closed (forbid all commands) instead of degrading to empty policy + warning (wrong-semantics)
  RUST: codex-rs/core/src/exec_policy.rs:565-568 load_exec_policy_with_warning maps `Err(ExecPolicyError::ParsePolicy { .. })` to `Ok((Policy::empty(), Some(err)))`, and network_proxy_loader.rs:61-73 likewise falls back to `codex_execpolicy::Policy::empty()` + a tracing::warn on parse failure. A bad .rules file therefore disables only the custom rules; commands are still evaluated by the normal heuristics/approval matrix.
  SWIFT: Sources/Tools/ExecPolicy.swift:211-218 load() catches any parse error and stores loadError; classify() at line 455-456 then returns `.forbidden` for EVERY command (`if loadError != nil { return .forbidden }`).
  DESC: A single malformed `$CODEX_HOME/rules/*.rules` (or exec_policy.rules) line will cause every command in the Swift port to be classified forbidden and denied, bricking the session, whereas upstream silently ignores the unparseable policy (logging a warning) and continues with default behavior. This is a behavioral correctness divergence a real client would hit if any rules file is hand-edited incorrectly.
  FIX: Make malformed-policy handling fail open with a logged warning, matching upstream load_exec_policy_with_warning.

1) In Sources/Tools/ExecPolicy.swift, change `load(codexHome:)` (211-218) to discard the parse error rather than storing it as a blanket-forbid flag, but still expose it for warning/diagnostics. Concretely, keep `loadError` for surfacing the warning but stop using it to forbid:
   - In `classify(argv:)` (455-456) delete the line `if loadError != nil { return .forbidden }`. With this removed, a failed parse yields an empty ExecPolicy (Rules() / no prefixRules / no hostExecutables), so classification falls through to the existing CommandSafety heuristic at line 462 (`CommandSafety.classify(argv:) == .safe ? .safe : .needsApproval`) — the Swift analog of upstream's render_decision_for_unmatched_command. This restores fail-open semantics: only the custom rules are dropped, commands still go through normal safety/approval.

2) Surface the warning (upstream logs `tracing::warn!("failed to parse rules: {err}")`). At the two call sites — Sources/codexd/main.swift:198 and Sources/codex-session/main.swift:200 — after `ExecPolicy.load(...)`, if the policy carries a loadError, log it (e.g. via the existing `log`/FileHandle.standardError mechanism already used nearby in codexd/main.swift). To enable this, add a public read accessor on ExecPolicy, e.g. `public var loadWarning: String? { loadError }`.

3) Update the now-inaccurate doc comment at ExecPolicy.swift:206-210 to state that invalid policy files degrade to an empty policy plus a warning (commands still evaluated by heuristics), not fail-closed.

Note: loadStrict can remain as a strict variant for any explicit "validate my rules" path (mirroring upstream check_execpolicy_for_warnings), but the default runtime loader used by the session must not forbid all commands.

[apply-patch] Add File contents drop the trailing newline upstream always appends (wrong-semantics)
  RUST: apply-patch/src/parser.rs:291-299 — for each add_line, `contents.push_str(line_to_add); contents.push('\n');`, so `+abc\n+def` yields contents `"abc\ndef\n"`. lib.rs:405 writes `contents.clone().into_bytes()` verbatim and lib.rs:413 sets FileChange::Add.content to that same trailing-newline-terminated string. parser.rs test line 608-609 asserts contents == "hi\n".
  SWIFT: Sources/Tools/ApplyPatch.swift:286 — `let disk = added.joined(separator: "\n")` with an explicit comment that it adds NO forced trailing newline; written to disk at line 85 and surfaced as newContents at line 84/146.
  DESC: Upstream produces Add-File content with a terminating newline after every added line (`abc\ndef\n`); Swift joins lines with `\n` and omits the final newline (`abc\ndef`). The on-disk file and the FileChange.Add content field therefore differ by one trailing newline for every added file.
  FIX: In Sources/Tools/ApplyPatch.swift, replace line 286's `let disk = added.joined(separator: "\n")` (and remove the misleading comment at lines 283-285) with logic that appends a trailing newline after every added line, matching upstream parser.rs:291-299. Concretely: `let disk = added.isEmpty ? "" : added.map { $0 + "\n" }.joined()` — i.e. `"hi"` -> `"hi\n"`, `["line1","line2"]` -> `"line1\nline2\n"`. (Equivalently `added.reduce(into: "") { $0 += $1 + "\n" }`.) Then update the test expectation at Tests/ToolsTests/ToolsTests.swift:27 from `"line1\nline2"` to `"line1\nline2\n"`, and verify the `+hi` add tests (ToolsTests.swift:60, :230) and EndToEndTests.swift:3504 (`+# Remote doc` -> `"# Remote doc\n"`) expect trailing-newline content. No change needed for empty Add File (zero `+` lines) since upstream yields empty string in that case too.

[apply-patch] Update applier omits trailing-empty-line pop before matching and final-newline re-add (wrong-semantics)
  RUST: apply-patch/src/lib.rs:670-684 — original split on '\n', then `if original_lines.last().is_some_and(String::is_empty) { original_lines.pop(); }` BEFORE compute_replacements, and after applying: `if !new_lines.last().is_some_and(String::is_empty) { new_lines.push(String::new()); }` to guarantee a trailing newline, then `new_lines.join("\n")`.
  SWIFT: Sources/Tools/ApplyPatch.swift:509-542 deriveNewContents — splits on "\n" with omittingEmptySubsequences:false but never pops the trailing empty element before seekSequence, and never re-adds a trailing empty line; just `lines.joined(separator: "\n")`.
  DESC: Upstream strips the synthetic trailing empty line produced by split('\n') so line indices/EOF matching mirror `diff`, and guarantees the rewritten file ends in a newline. Swift keeps the trailing empty element (shifting EOF/seek behavior and the chunk.is_end_of_file search-start) and can emit a file with no trailing newline. This changes both match positions and the resulting file bytes / new_content wire field.
  FIX: In Sources/Tools/ApplyPatch.swift deriveNewContents (line 509-512), pop the synthetic trailing empty element before matching, and re-add a guaranteed trailing newline before joining:

    var lines = original
        .split(separator: "\n", omittingEmptySubsequences: false)
        .map(String.init)
    if lines.last == "" { lines.removeLast() }   // mirror lib.rs:674-676
    var lineIndex = 0
    ... (existing loop) ...
    if lines.last != "" { lines.append("") }      // mirror lib.rs:681-683
    return lines.joined(separator: "\n")

Also, to fully match upstream, add the pure-addition branch for empty oldLines (lib.rs:722-731): when chunk.oldLines.isEmpty, insert chunk.newLines at (lines.last == "" ? lines.count-1 : lines.count) instead of relying on seekSequence returning lineIndex. After the change, re-run the existing ApplyPatch tests (the "line1\nline2" add test referenced in the comment at line 285 is for the Add path, not this Update path, so it is unaffected).

[apply-patch] Pure-addition chunks (empty old_lines) inserted at running index instead of end-of-file (wrong-semantics)
  RUST: apply-patch/src/lib.rs:722-731 — when `chunk.old_lines.is_empty()`, upstream computes `insertion_idx = if original_lines.last is empty { len-1 } else { len }` and pushes `(insertion_idx, 0, new_lines)`, i.e. appends at end of file (before any trailing newline).
  SWIFT: Sources/Tools/ApplyPatch.swift:522-539 — no special case for empty oldLines; calls seekSequence(lines, [], lineIndex, eof) which returns `start` (line 472 short-circuits on empty pattern), then replaceSubrange(at..<at, with: newLines) inserts at lineIndex (top/running position), not end-of-file.
  DESC: For an Update chunk that only adds lines (no context, no removals), upstream appends them at the end of the file; Swift inserts them at the current line index (effectively the file start for a first chunk). This produces different file content for pure-addition update hunks.
  FIX: In Sources/Tools/ApplyPatch.swift, in deriveNewContents, add an empty-oldLines special case at the top of the `for chunk in chunks` loop body, mirroring upstream lib.rs:722-731. After the changeContext seek (lines 515-520) and before `let pattern = chunk.oldLines` (line 522), insert:

    if chunk.oldLines.isEmpty {
        // Pure addition: append at end of file, before a trailing empty line if present.
        let insertionIdx = (lines.last == "") ? lines.count - 1 : lines.count
        lines.insert(contentsOf: chunk.newLines, at: insertionIdx)
        lineIndex = insertionIdx + chunk.newLines.count
        continue
    }

This makes pure-addition update chunks append at EOF (matching upstream's insertion_idx computation) instead of inserting at the running line index. Note: upstream computes all replacements first, sorts by index, then applies; the simpler in-place insert above is equivalent for the typical single/ordered-chunk case, but if exact multi-chunk ordering parity is required, replicate upstream's collect-then-sort-by-start-index-then-apply structure. (For full fidelity also consider that upstream appends at EOF regardless of any preceding change_context; the snippet above does too since it ignores lineIndex.)

[persistence-rollout] Rollout filename and directory layout diverge from upstream (no `rollout-` prefix, no date-partitioned dirs, no timestamp in name) (wrong-semantics)
  RUST: codex-rs/rollout/src/recorder.rs:1329-1348 precompute_log_file_info builds `~/.codex/sessions/<year>/<MM>/<DD>/rollout-<YYYY-MM-DDThh-mm-ss>-<conversation_id>.jsonl`; codex-rs/rollout/src/list.rs:898/918/988 require `name.starts_with("rollout-") && name.ends_with(".jsonl")` and list.rs:930-932 `parse_timestamp_uuid_from_filename` strips `rollout-` prefix to derive created_at+uuid used for sorting/cursors.
  SWIFT: Sources/Persistence/ThreadStore.swift:98-100 `rolloutPath` = `codexHome + "/sessions/\(id.raw).rollout.jsonl"` — flat directory, `<uuid>.rollout.jsonl`, no `rollout-` prefix, no embedded timestamp, no YYYY/MM/DD partitioning. Same scheme echoed at SessionEngine.swift:1860 and RequestRouter.swift:4332.
  DESC: Upstream derives a thread's created_at, sort key, and listing-cursor entirely from the filename (`rollout-<ts>-<uuid>.jsonl`) under date-partitioned directories. Swift writes a flat `<uuid>.rollout.jsonl`. An external upstream codex CLI / state-DB backfill scanning `~/.codex/sessions` would not recognize Swift files as rollouts at all (prefix/suffix check fails), and even if found could not parse a timestamp from the name.
  FIX: Make the on-disk rollout layout match upstream so files are discoverable by upstream tooling, while keeping the SQLite DB as the fast index. Concretely, in Sources/Persistence/ThreadStore.swift change rolloutPath to produce the upstream layout. Because upstream derives created_at FROM the filename, the timestamp must be stable per thread, so persist it: (1) Generate the path once at create() time using the thread's creation timestamp `t` and store the full path in the `rolloutPath` column (already a column in StateDB, StateDB.swift:25) — then everywhere else read the path from the DB row instead of recomputing it. Replace the private `rolloutPath(_ id:)` recomputation with a builder used only at creation: `func makeRolloutPath(_ id: ThreadId, createdAt: Int64) -> String` that formats `sessions/<YYYY>/<MM>/<DD>/rollout-<YYYY-MM-DDThh-mm-ss>-<id.raw>.jsonl` (local time, `-` separators, matching recorder.rs:1338-1346), creating intermediate dirs via FileManager.createDirectory(withIntermediateDirectories: true). (2) Update the two other hard-coded sites — SessionEngine.swift:1853-1860 and RequestRouter.swift:4332 — to look up the stored path from the StateDB ThreadRow (db.getThread(id).rolloutPath) rather than reconstructing `sessions/<uuid>.rollout.jsonl`, since they can no longer derive the path from the id alone. (3) For backward compatibility with already-written flat files, have the path lookups fall back to the legacy `sessions/<id>.rollout.jsonl` if the DB has no stored path / the new path does not exist. This preserves the existing DB-driven listing while making the filesystem layout byte-compatible with upstream's list.rs scanner (date dirs + `rollout-<ts>-<uuid>.jsonl`).

[persistence-rollout] General thread items persisted in Swift-proprietary `{"t":"item"}` envelope instead of upstream `response_item` rollout line (wrong-semantics)
  RUST: codex-rs/protocol/src/protocol.rs:2783-2791 `RolloutItem` is `#[serde(tag="type", content="payload", rename_all="snake_case")]` with variant `ResponseItem(ResponseItem)` → on-disk `{"timestamp":...,"type":"response_item","payload":<ResponseItem>}`. policy.rs should_persist_response_item persists Message/Reasoning/FunctionCall/LocalShellCall/etc. rollout_reconstruction.rs replays `RolloutItem::ResponseItem` to rebuild history.
  SWIFT: Sources/Persistence/Rollout.swift:639-641 — for `.item` (and `.environmentRebound`) `rustRolloutLine` returns nil, so encodeLine (Rollout.swift:443-450) falls back to the proprietary Codable encoding `{"t":"item","turnId":...,"item":{...}}`. Only `.item(_, .agentMessage)` is mapped to an upstream `event_msg`/`agent_message` line (Rollout.swift:538-545); reasoning, command executions, file changes, tool calls, context messages, and user messages-as-items are NOT emitted as `response_item`. Grep confirms no `"type":"response_item"` is ever written.
  DESC: Upstream's model-visible history (assistant/user messages, reasoning, tool calls, function-call outputs) is the bulk of a rollout and is persisted as `response_item` lines, which the reconstruction replay consumes. Swift writes these as a private `{"t":"item"}` shape, so an upstream codex replaying the file rebuilds an essentially empty history. The Swift reader handles its own shape, so Swift-internal resume works; cross-tool/full-fidelity replay does not.
  FIX: In Sources/Persistence/Rollout.swift:

1) WRITE path — replace the catch-all `case .item, .environmentRebound: return nil` (line 639-640). For .item(let tid, let item), emit an upstream response_item line for model-visible variants, mirroring upstream should_persist_response_item (policy.rs:67). Concretely:
   case .item(_, let item):
       // Skip variants upstream does not persist as ResponseItem (none currently
       // map to CompactionTrigger/Other; .contextCompaction maps to ResponseItem::ContextCompaction = persisted).
       return ["timestamp": rustTimestamp(), "type": "response_item",
               "payload": Self.threadItemToResponseItem(item)]
   case .environmentRebound: return nil   // keep as-is (no upstream analog)

   Note: keep emitting the existing agent_message event_msg sidecar IF a display-event stream is also desired (upstream emits both), but the response_item line is the load-bearing model-history record. Simplest correct change: emit response_item for ALL item variants and additionally keep the agent_message event via sidecarLines() if needed for display-event parity. Do NOT keep the current behavior where .agentMessage ONLY produces an event_msg.

2) Make threadItemToResponseItem total (Rollout.swift:737-749): replace the preconditionFailure for .commandExecution / .fileChange / .contextCompaction with real mappings so general persistence does not crash:
   - .commandExecution(id, command, ...) -> {"type":"function_call","id":id,"name":"shell","call_id":id,"arguments": <json string of {"command":command,"cwd":cwd}>} (or "local_shell_call" per upstream ResponseItem::LocalShellCall shape) plus a paired function_call_output if status==completed. Match upstream ExecCommand/LocalShellCall ResponseItem serialization.
   - .fileChange(id, changes, ...) -> the upstream ResponseItem used for apply_patch (function_call with name "apply_patch" / custom_tool_call) preserving the diff.
   - .contextCompaction(id) -> {"type":"context_compaction","id":id} (upstream ResponseItem::ContextCompaction).
   Verify exact field names against codex-rs models::ResponseItem serde definitions before finalizing.

3) READ path — add a "response_item" case to rustRolloutRecords (Rollout.swift:842 switch, before default): 
   case "response_item":
       guard let item = Self.responseItemToThreadItem(payload), let tid = currentTurnId ?? ... else { return [] }
       return [.item(turnId: tid, item: item)]
   Reuse the existing responseItemToThreadItem (Rollout.swift:927-972), which already maps message/reasoning/unknown and preserves tool-call payloads as .unknown. Use the running currentTurnId cursor for turn association (response_item payloads carry no turn_id upstream).

Add a round-trip test: write each ThreadItem variant, assert the on-disk line is {"type":"response_item","payload":{"type":...}} (snake_case discriminator), and assert an upstream-shaped response_item line is read back into the correct ThreadItem.

[persistence-rollout] `turn_context` rollout line omits upstream-required fields (approval_policy, sandbox_policy, summary) (wrong-semantics)
  RUST: codex-rs/protocol/src/protocol.rs:2823-2860 `TurnContextItem` declares `pub approval_policy: AskForApproval`, `pub sandbox_policy: SandboxPolicy`, `pub model: String`, and `pub summary: ReasoningSummaryConfig` as REQUIRED (non-Option, no serde default). recorder.rs:842 deserializes each line via `serde_json::from_value::<RolloutLine>`; a turn_context payload missing these fields fails to deserialize and is counted as a parse error (recorder.rs:865-868) and dropped.
  SWIFT: Sources/Persistence/Rollout.swift:620-638 emits `turn_context` payload with only `cwd`, `model`, `turn_id`, and optional `current_date`/`timezone`/`realtime_active`. Comment at Rollout.swift:626-629 explicitly defers `approval_policy`, `sandbox_policy`, `summary`.
  DESC: Because upstream treats approval_policy/sandbox_policy/summary as mandatory on TurnContextItem, an upstream reader will fail to parse every Swift-written `turn_context` line and skip it. That loses the per-turn baseline (model, cwd) that upstream `resume_candidate_matches_cwd` and reconstruction rely on, defeating the very CWD-match purpose the Swift code documents for this record.
  FIX: In Sources/Persistence/Rollout.swift, extend the `.turnContext` case to carry and emit the three required upstream fields. Concretely: (1) Add `approvalPolicy: String`, `sandboxPolicy: [String: Any]` (or a small enum), and `summary: String` (default "auto") to the `.turnContext` enum case (Rollout.swift:33) and thread them through the Codable encode/decode (lines 158-159, 244-255), the parse path (897-915), and the persist call site (SessionEngine.swift:1090-1092). (2) In the emit at Rollout.swift:630-638, add to payload: `payload["approval_policy"]` = upstream kebab/renamed string for config.approvalPolicy (untrusted | on-failure | on-request | never, matching AskForApproval #[serde(rename...)] at protocol.rs:900-925); `payload["sandbox_policy"]` = a tagged object `{"type": "read-only"}` / `{"type":"workspace-write", ...}` / `{"type":"danger-full-access"}` matching SandboxPolicy #[serde(tag="type", rename_all="kebab-case")] at protocol.rs:993-1019 (note workspace-write needs writable_roots/network_access sub-fields, read-only optionally network_access); `payload["summary"]` = lowercase ReasoningSummary value ("auto" default, or concise/detailed/none) per config_types.rs:30-38. Map config.sandboxMode (.readOnly/.workspaceWrite/.dangerFullAccess) and config.networkAccess/config.writableRoots into the sandbox_policy object. This makes Swift-written turn_context lines deserialize successfully under upstream serde_json::from_value::<RolloutLine>, preserving the per-turn baseline for resume/fork replay.

[config] config/read emits synthetic default keys and forces model/approval/sandbox defaults that upstream leaves unset (wrong-default)
  RUST: app-server-protocol/src/protocol/v2/config.rs:249-285 the v2 Config struct fields are all Option<...> (model, approval_policy, sandbox_mode, ...) and there is NO allow_login_shell / project_doc_max_bytes / project_doc_fallback_filenames / hide_agent_reasoning / history field. app-server/src/request_processors/config_processor.rs:87-114 read() maps the merged config; unset fields serialize to null/omitted (no synthesized model/policy).
  SWIFT: Sources/Config/Config.swift:302-317 defaults() injects model:"gpt-5.5", approval_policy:"on-request", sandbox_mode:"workspace-write", allow_login_shell:true, history:{persistence:"save-all"}, project_doc_max_bytes, project_doc_fallback_filenames:[], hide_agent_reasoning:false as a real layer; RequestRouter.swift:1737-1743 returns merged config verbatim via config/read.
  DESC: When the user has no model/approval_policy/sandbox_mode configured, upstream config/read returns those fields as null; Swift returns "gpt-5.5"/"on-request"/"workspace-write". Swift also emits extra keys (allow_login_shell, project_doc_max_bytes, project_doc_fallback_filenames, hide_agent_reasoning, history) that are not part of the v2 Config schema and would land in the additional/flatten bucket or mislead a frontend. The "gpt-5.5" model string is not a verified upstream default either.
  FIX: Scope the fix to the config/read wire shape and to only model/approval_policy/sandbox_mode; do NOT remove the other five keys (they match upstream).

Option A (preferred, minimal, runtime-safe): in RequestRouter.swift .configRead handler (around line 1737), after building `configJSON`, drop the three keys when they originate solely from the synthetic defaults layer (i.e. when no real layer set them). Use `effectiveConfig.origins` to detect this: for key in ["model","approval_policy","sandbox_mode"], if origins[key] == "defaults" { set configJSON[key] = .null } (or remove and let it be null). This makes config/read return null for an unconfigured user (matching upstream) while keeping the runtime defaults layer intact for SessionEngine, which depends on a concrete model.

Option B: remove the three entries from Config.swift `defaults()` (lines 304-306) entirely and instead supply runtime fallbacks at the SessionEngine/PromptAssembly call sites that currently assume a non-nil model. This is the more faithful structural fix (upstream has no defaults layer at all for these) but is higher-risk because multiple call sites use config.model non-optionally; only do this if those fallbacks are added.

Also reconsider the literal "gpt-5.5" default string — it is not a verified upstream default; cross-check against the actual upstream default model before retaining it as the runtime fallback.

Do NOT remove allow_login_shell, project_doc_max_bytes, project_doc_fallback_filenames, hide_agent_reasoning, or history — upstream emits these and removing them would CREATE a new divergence. The only borderline nit: upstream's history serializes with an explicit "max_bytes": null; Swift omits it. Optionally add "max_bytes": .null inside the history default object to match byte-for-byte, but this is cosmetic.

[config] config/read ignores cwd and includeLayers params (no project layers, layers always null) (wrong-semantics)
  RUST: app-server-protocol/src/protocol/v2/config.rs:357-365 ConfigReadParams{include_layers, cwd}; config_processor.rs:91-93 uses params.cwd to load project layers (load_latest_config(fallback_cwd)); ConfigReadResponse.layers (config.rs:374-375) is Some(Vec<ConfigLayer>) when include_layers is true.
  SWIFT: Sources/Supervisor/RequestRouter.swift:1722-1743 case .configRead(let id, _) discards params entirely; builds ConfigLoader(codexHome: codexHome) with no cwdOverride (so loadProjectLocalLayers returns [] per Config.swift:494), and always returns "layers": .null.
  DESC: Upstream returns the effective config as seen from the supplied cwd (including project layers between cwd and repo root) and returns a populated layers array when includeLayers is true. Swift never honors cwd (project-local .codex/config.toml never applied for config/read) and never returns layers, so a frontend requesting layer detail or a directory-scoped view gets neither.
  FIX: In Sources/Config/Config.swift, surface the constructed layers from load(). Either (a) add a stored `public let layers: [ConfigLayer]` to Config and set it in init, or (b) add an overload `func loadWithLayers(...) -> (Config, [ConfigLayer])`. The layers must be filtered to non-empty values and ordered to mirror upstream's get_layers(HighestPrecedenceFirst) when serialized.

In Sources/Supervisor/RequestRouter.swift:1722, change the handler to use params:
  case .configRead(let id, let params):
    let loader = ConfigLoader(codexHome: codexHome, cwdOverride: params.cwd)
    let effectiveConfig = loader.load()   // now cwd-scoped: project .codex layers applied when cwd present
    ... build configJSON/origins as today ...
    let layersJSON: JSONValue = (params.includeLayers ?? false)
        ? .array(effectiveConfig.layers /* or returned layer list */
            .reversed()                  // highest precedence first, matching upstream
            .map { layer in .object([
                "name": .string(layer.name),
                // map to upstream ConfigLayer wire shape (kind/origin/path/values)
                "values": .object(layer.values.mapValues(toJSON))
            ]) })
        : .null
    await reply(conn, id, .object(["config": .object(configJSON),
                                   "origins": .object(...),
                                   "layers": layersJSON]))

Note the ConfigLayer wire shape: verify against app-server-protocol ConfigLayer.as_layer() (schema/typescript/v2/ConfigLayer.ts / common.rs) and emit matching fields rather than the minimal {name,values} above. Also update the GenericResponses.swift:254 fallback consistently if it is ever exercised. Add a test that passes cwd pointing at a dir with a .codex/config.toml and asserts the value overrides through, plus includeLayers:true returning a populated array.

[config] config write does not return structured config_write_error_code error data (missing-error-code)
  RUST: app-server/src/request_processors/config_processor.rs:600-614 map_error/config_write_error attach error.data = {"config_write_error_code": code} where code is one of ConfigWriteErrorCode (config.rs:342-352): ConfigLayerReadonly, ConfigVersionConflict, ConfigValidationError, ConfigPathNotFound, ConfigSchemaUnknownKey, UserLayerNotFound (camelCase on the wire).
  SWIFT: Sources/Supervisor/RequestRouter.swift:5717-5727,5749-5751 returns WireError.invalidRequest(id, message) with only a human string for version-conflict / disallowed-path / parse errors; no data.config_write_error_code field is ever emitted.
  DESC: Upstream callers (frontend) key off error.data.config_write_error_code to distinguish e.g. a version conflict (configVersionConflict) from a validation error to drive retry/UX. Swift only sends a free-text invalidRequest message, so the frontend cannot programmatically classify the failure.
  FIX: In Sources/Supervisor/RequestRouter.swift, replace the bare WireError.invalidRequest emissions in handleConfigWriteRequest with an error that carries data.config_write_error_code (camelCase wire values matching upstream). Concretely:

1. Add a helper near WireError (or inline in RequestRouter), e.g.:
   static func configWriteError(id: RequestId, code: String, _ message: String) -> JSONRPCMessage {
       .error(JSONRPCError(id: id, error: .init(code: WireError.invalidRequestCode, message: message,
           data: .object(["config_write_error_code": .string(code)]))))
   }

2. Map each failure site to the correct code:
   - Line 5718 (path != configPath): code "configLayerReadonly" (upstream ConfigLayerReadonly, config_manager_service.rs:209-212), keep message "Only writes to the user config are allowed".
   - Line 5725 (expectedVersion mismatch): code "configVersionConflict", message "Configuration was modified since last read. Fetch latest version and retry."
   - Line 5750 (catch on parse/merge/validation): code "configValidationError", message error.localizedDescription. Upstream wraps validation failures with "Invalid configuration: ..." but the load-bearing wire field is config_write_error_code, so emitting configValidationError satisfies the contract. (If the thrown error can distinguish missing user layer, also support "userLayerNotFound".)

Note the enum is serialized camelCase upstream, so use the lowerCamel string values exactly: configLayerReadonly, configVersionConflict, configValidationError, configPathNotFound, configSchemaUnknownKey, userLayerNotFound. The JSONRPCErrorObject.data field (Sources/WireProtocol/JSONRPC.swift:67) already encodes omit-if-nil, so no protocol changes are needed.

[config] config write never reports okOverridden status or overriddenMetadata (wrong-semantics)
  RUST: app-server/src/config_manager_service.rs:316-336 computes first_overridden_edit and sets status = OkOverridden with Some(overridden_metadata) when a higher-precedence layer overrides the just-written key; ConfigWriteResponse (config.rs:334-340) carries status (WriteStatus Ok|OkOverridden -> "ok"|"okOverridden") and overridden_metadata {message, overriding_layer, effective_value}.
  SWIFT: Sources/Supervisor/RequestRouter.swift:5743-5748 hardcodes status:"ok" and overriddenMetadata:.null unconditionally.
  DESC: When a user writes a key that is shadowed by a higher-precedence layer (system/managed/session), upstream returns status okOverridden plus overriddenMetadata so the UI can warn the write has no effect. Swift always claims plain "ok" with null metadata, silently misreporting overridden writes.
  FIX: In Sources/Supervisor/RequestRouter.swift (config write handler, around lines 5730-5748), after persisting the user TOML, load the full layer stack and compute override metadata per written key, mirroring upstream compute_override_metadata/find_effective_layer:

1. let stack = ConfigLoader(codexHome: codexHome).load()  // gives merged effective + origins + layers
2. For each written (keyPath, value, _) edit, in order:
   - userValue: read the value at the dotted keyPath from the user layer only. Either re-read the persisted user TOML (Self.readUserConfigTOML(path:) -> walk segments) or expose the user `toml` ConfigLayer from the loader.
   - effectiveValue: stack.value(keyPath) (the deep-merged effective value).
   - Skip (no override) if userValue == effectiveValue, or if both are nil.
   - Otherwise find the highest-precedence layer whose values define the keyPath (iterate the layer stack reversed — highest precedence first — and return the first whose values contain the segments). That layer's name is the overridingLayer.
3. If any edit is overridden, take the FIRST overridden edit and reply with:
   "status": .string("okOverridden"),
   "overriddenMetadata": .object([
       "message": .string(overrideMessage(forLayer: layerName)),   // mirror upstream override_message wording per layer (system/managed/session/etc.)
       "overridingLayer": <ConfigLayerMetadata JSON matching upstream ConfigLayerMetadata: name + path/version fields the v2 schema requires>,
       "effectiveValue": toJSON(effectiveValue ?? .null)
   ])
   else keep "status": .string("ok"), "overriddenMetadata": .null.

Also map Swift's internal layer names (defaults/system/toml/profile-v2/<project>/env/override in Config.swift:659-673) onto the upstream ConfigLayerMetadata layer identifiers so overridingLayer.name matches what the UI expects (system/managed/session/project/etc.). Add a helper override_message equivalent matching upstream's per-layer text. Note: the user-layer value lookup must use ONLY the user toml layer, not the merged config, to match upstream's get_active_user_layer semantics.

[config] Project-local config layers applied without the upstream trust gate (wrong-semantics)
  RUST: config/src/loader/mod.rs:1162-1169,828-844 each .codex/config.toml layer gets trust_context.decision_for_dir; when not Trusted the layer is created via ConfigLayerEntry::new_disabled (disabled_reason set) and excluded from the effective config; trust comes from [projects.<path>] trust_level in user config.
  SWIFT: Sources/Config/Config.swift:490-558 loadProjectLocalLayers loads and applies every discovered .codex/config.toml unconditionally (only the denylist is applied); grep shows no trust_level/projects-trust check anywhere in Sources/Config or the loader path.
  DESC: Upstream treats project-local config as untrusted-by-default: an unrusted repo's .codex/config.toml is loaded-but-disabled and does not affect the effective config until the user marks the project trusted. Swift applies untrusted project-local config to the effective config immediately, a security/correctness regression (a checked-out repo can silently change settings within the denylist boundary).
  FIX: Implement an upstream-equivalent project-trust gate in Sources/Config/Config.swift. Concretely: (1) Parse `[projects]` from the already-loaded user config (userValues) into a map of normalized path -> trust_level string (e.g. "trusted"/"untrusted"), matching upstream project_trust_for_lookup_key / decision_for_dir keyed on the dir (and repo-root) path. (2) In loadProjectLocalLayers, for each candidate `dir`, compute a trust decision from that map (default = not trusted when no entry). When NOT trusted, mark the layer disabled — either skip appending it entirely (simplest equivalent, since Swift has no include_disabled consumers) or carry an `isDisabled`/`disabledReason` field on the returned tuple/ConfigLayer and exclude disabled layers when building the effective stack at lines 659-667 (mirroring state.rs layers_high_to_low filtering with include_disabled=false). (3) Only project layers whose dir resolves to a Trusted decision should reach the effective `layers` array. Keep the denylist applied on top of (not instead of) the trust gate. Add a test: an untrusted repo's .codex/config.toml must NOT change the effective config, and the same repo marked `[projects."/path"] trust_level = "trusted"` in user config must apply it.

[prompts] on_request approval prompt omits the request_permissions tool section (prompt-text)
  RUST: codex-rs/core/src/context/permissions_instructions.rs:178-201 `on_request_instructions()` and the OnFailure/UnlessTrusted arms append `request_permissions_tool_prompt_section()` (lines 258-260: `# request_permissions Tool\n\nThe built-in \`request_permissions\` tool is available...`) whenever `request_permissions_tool_enabled` is true.
  SWIFT: Sources/Prompts/Permissions.swift:60-118 `approvalOnRequest` emits only the bare on_request.md text and the on-failure/unless-trusted arms (lines 57-59) emit only their bare md; the `# request_permissions Tool` section string does not exist anywhere in Sources/ (grep confirms no match). Swift comment lines 187-196 explicitly states this is deferred.
  DESC: When `request_permissions_tool_enabled` is true (and the Swift default `defaultRequestPermissionsToolEnabled = true`), upstream appends a `# request_permissions Tool` instruction block to the on-request / on-failure / unless-trusted approval preambles. Swift never appends it, so the developer-role permissions instructions diverge from upstream whenever the request_permissions tool is active.
  FIX: In Sources/Prompts/Permissions.swift:

1. Add the verbatim section constant (matching upstream lines 258-260):
   static let requestPermissionsToolSection = "# request_permissions Tool\n\nThe built-in `request_permissions` tool is available in this session. Invoke it when you need to request additional `network` or `file_system` permissions before later shell-like commands need them. Request only the specific permissions required for the task."

2. In `approvalText` (lines 164-172), append the section to the on-request/on-failure/unless-trusted arms when `requestPermissionsToolEnabled` is true (Never must NOT append). Mirror upstream: for `.unlessTrusted` and `.onFailure`, wrap base as `requestPermissionsToolEnabled ? "\(base)\n\n\(requestPermissionsToolSection)" : base`. For `.onRequest`, build a sections array `[approvalOnRequest]` and, when enabled, append `requestPermissionsToolSection`, joined by "\n\n" (upstream pushes it as a separate section in `on_request_instructions()`).

3. In `granularInstructions` (lines 224-235), after the rejected-categories block, append `requestPermissionsToolSection` when `requestPermissionsToolEnabled && cfg.requestPermissions` (upstream `request_permissions_tool_prompts_allowed`, lines 271-272 / 321-323).

Note: full upstream parity also requires plumbing `exec_permission_approvals_enabled` (to choose on_request_rule_request_permission.md and the inline shell-permission guidance) and approved-command-prefixes text, but those are a separate, broader gap already flagged in the Swift comments; the minimal fix for THIS finding is the three changes above.

[prompts] on_request rule-request-permission body and approved-prefix listing not emitted (prompt-text)
  RUST: codex-rs/core/src/context/permissions_instructions.rs:179-192 selects `APPROVAL_POLICY_ON_REQUEST_RULE_REQUEST_PERMISSION` (the `on_request_rule_request_permission.md` `# Permission Requests` body) when `exec_permission_approvals_enabled`, and appends `## Approved command prefixes\nThe following prefix rules have already been approved: {prefixes}` (lines 188-192, 325-329). Granular path also pushes the rule-request body when shell permission requests are available (line 317-319).
  SWIFT: Sources/Prompts/Permissions.swift only ships `approvalOnRequest` = the simpler on_request.md; there is no `# Permission Requests` / `with_additional_permissions` / `## Approved command prefixes` text in Sources/ (grep: only an unrelated ProtocolModel/Approvals.swift comment mentions `with_additional_permissions`). granularInstructions() (lines 196-236) never appends the on_request_rule_request_permission body.
  DESC: When exec-permission-approvals are enabled, upstream swaps in the longer `on_request_rule_request_permission.md` body and appends an approved-command-prefixes section; the Swift granular path also should append that body when sandbox approvals are allowed. Neither is reproduced, so the approval preamble text diverges in those modes.
  FIX: In Sources/Prompts/Permissions.swift:

1. Add the verbatim on_request_rule_request_permission.md body as a new static constant, e.g.:
   static let approvalOnRequestRuleRequestPermission = #"""\n# Permission Requests\n\nCommands may require user approval before execution. Prefer requesting sandboxed additional permissions instead of asking to run fully outside the sandbox.\n... (full md verbatim, trim_end'd) ...\nEach segment is evaluated independently for sandbox restrictions and approval requirements.\n"""#
   (copy exact text from codex-rs/core/src/context/prompts/permissions/approval_policy/on_request_rule_request_permission.md).

2. Thread two new pieces of data into PermissionsInstructions: an `execPermissionApprovalsEnabled: Bool` flag and the approved-prefix list (or a closure/array of allowed prefixes formatted like upstream format_allow_prefixes). Add them to the init signature and to approvalText()/granularInstructions().

3. In approvalText() for .onRequest, mirror on_request_instructions(): pick approvalOnRequestRuleRequestPermission when execPermissionApprovalsEnabled else approvalOnRequest; then if requestPermissionsToolEnabled append the request_permissions tool section; then if approvedPrefixes is non-empty append "## Approved command prefixes\nThe following prefix rules have already been approved: \(prefixes)". Join with "\n\n".

4. In granularInstructions(), after the rejected list: when (execPermissionApprovalsEnabled && cfg.sandboxApproval) append approvalOnRequestRuleRequestPermission; when requestPermissionsToolEnabled && cfg.requestPermissions append the request_permissions tool section; when approvedPrefixes non-empty append the "## Approved command prefixes" section — matching granular_instructions() L317-329.

5. Add the request_permissions tool section constant (request_permissions_tool_prompt_section text) since it is referenced by both paths and is also currently missing.

6. Update SessionEngine.swift:171 to pass the session's exec_permission_approvals_enabled flag, request-permissions-tool-enabled flag, and the resolved allowed-prefix list (from the ExecPolicy/ApprovedRuleStore already available in init) into the PermissionsInstructions constructor.

7. Remove/replace the misleading doc comment at L182-195 that claims inline-escalation guidance is emitted.

[mcp] initialize handshake omits the elicitation client capability (wrong-semantics)
  RUST: codex-mcp/src/rmcp_client.rs:474-492 sets ClientCapabilities { elicitation: Some(client_elicitation_capability), .. } in InitializeRequestParams; connection_manager_tests.rs:850-872 assert the wire shape serializes capabilities.elicitation as `{}` (form-only) or `{"form":{},"url":{}}`.
  SWIFT: Sources/MCP/McpClient.swift:666-669 and Sources/MCP/McpHttpClient.swift:664-668 send `"capabilities": [String: Any]()` — an empty object with no `elicitation` key.
  DESC: Upstream always advertises an `elicitation` capability in the initialize request so the server knows the client can handle elicitation/create. Swift advertises an empty capabilities object. A spec-compliant MCP server that gates elicitation/create on the client having declared the elicitation capability will never send elicitation requests, silently disabling the entire elicitation feature for those servers — even though Swift has the elicitation/create handling code wired up.
  FIX: In both MCP client initialize methods, replace the empty capabilities object with one that includes the elicitation key. To match upstream's default (AuthElicitation off) behavior precisely, emit elicitation as an empty object: in Sources/MCP/McpClient.swift:666-669 and Sources/MCP/McpHttpClient.swift:664-668, change "capabilities": [String: Any]() to "capabilities": ["elicitation": [String: Any]()]. (If/when an AuthElicitation-equivalent feature is enabled, emit ["elicitation": ["form": [String: Any](), "url": [String: Any]()]] instead, matching session.rs:1028-1031.) Add a test asserting the serialized initialize params contain capabilities.elicitation as {} to lock the wire shape, mirroring connection_manager_tests.rs:850-857.

[mcp] resources/list and resources/templates/list are not implemented in the MCP client (missing-method)
  RUST: rmcp-client/src/rmcp_client.rs:497-527 implements list_resources ("resources/list") and list_resource_templates ("resources/templates/list"); connection_manager.rs:447-579 paginate and aggregate them per server with duplicate-cursor protection, exposed via list_all_resources / list_all_resource_templates and surfaced in collect_mcp_server_status_snapshot (core/src/mcp.rs:569-605).
  SWIFT: Sources/MCP/McpClient.swift:336-344 McpClientProtocol exposes only readResource — no listResources/listResourceTemplates. Sources/Supervisor/RequestRouter.swift:3957-3958 hardcodes `"resources": []` and `"resourceTemplates": []` in the server-status snapshot; nothing ever calls resources/list or resources/templates/list.
  DESC: Upstream enumerates resources and resource templates from each MCP server (with cursor pagination) and includes them in the ListMcpServerStatus response. The Swift client has no method for either RPC and always reports empty resources/resourceTemplates, so a frontend can never see or pick MCP resources/templates.
  FIX: Implement resource listing end-to-end:
1) Sources/MCP/McpClient.swift: add to McpClientProtocol and McpClient: `func listResources(cursor: String?) async throws -> (resources: [JSONLite], nextCursor: String?)` calling request("resources/list", cursor==nil ? [:] : ["cursor": cursor]) and `func listResourceTemplates(cursor:)` calling request("resources/templates/list", ...). Parse the "resources"/"resourceTemplates" and "nextCursor" fields from the result (mirror the existing readResource at lines 711-712).
2) Sources/MCP/McpManager.swift: extend McpServerStatus (line 45) with `var resources: [JSONValue]` and `var resourceTemplates: [JSONValue]`, and in statusList() paginate each server's listResources/listResourceTemplates (loop on nextCursor, with duplicate-cursor guard as in upstream connection_manager.rs) populating them.
3) Sources/Supervisor/RequestRouter.swift:3957-3958: replace the hardcoded `.array([])` with `.array(status.resources)` and `.array(status.resourceTemplates)` from the McpServerStatus.
4) Sources/ProtocolModel/V2.swift:291-298: add `resources: [JSONValue]` and `resourceTemplates: [JSONValue]` to McpServerStatusInfo so the Codable shape matches the upstream required-field schema.
Optionally (separate, larger item) add the agent-facing list_mcp_resources/list_mcp_resource_templates/read_mcp_resource tools to match core/src/tools/handlers/mcp_resource.

[mcp] Server-status snapshot reports a static authStatus of notLoggedIn for every server (wrong-semantics)
  RUST: codex-mcp/src/mcp/auth.rs:130-211 compute_auth_statuses derives per-server McpAuthStatus (Unsupported / NotLoggedIn / BearerToken / OAuth) from transport, bearer_token_env_var, stored OAuth tokens (determine_streamable_http_auth_status) and runtime auth; core/src/mcp.rs:604 puts these into the snapshot.
  SWIFT: Sources/Supervisor/RequestRouter.swift:3959 hardcodes `"authStatus": .string("notLoggedIn")` for all servers.
  DESC: Upstream computes a real auth status per server (e.g. bearerToken when a bearer_token_env_var is set, oauth when valid stored OAuth tokens exist, unsupported for stdio). Swift always emits notLoggedIn. A frontend cannot distinguish a logged-in OAuth/bearer server from one that needs login, and will incorrectly prompt for login on already-authenticated servers. (Field name authStatus and camelCase enum value notLoggedIn are themselves correct per app-server v2 McpAuthStatus camelCase serialization — only the value is wrong.)
  FIX: 1) Carry config into status: add the resolved McpServerConfig (or at least transport/url/bearerTokenEnvVar/httpHeaders/enabled) to McpServerStatus in Sources/MCP/McpManager.swift:45-50, and populate it in startAll (around lines 343-348) from the cfg already in scope.\n2) Add a computeAuthStatus helper (e.g. in McpManager or a small McpAuthStatus.swift) mirroring upstream compute_auth_status + determine_streamable_http_auth_status: if !enabled -> \"unsupported\"; stdio transport (no url) -> \"unsupported\"; if bearerTokenEnvVar != nil or an Authorization header present in resolved http_headers/env_http_headers -> \"bearerToken\"; else if McpOAuthStore(codexHome:).load(server: name) != nil -> \"oauth\"; else if OAuth discovery (McpOAuth.discoverMetadata over the streamable url) yields authorization+token endpoints -> \"notLoggedIn\"; else -> \"unsupported\". (Discovery can be best-effort/cached; falling back to a tokens-only check is acceptable if a network call at status time is undesirable, but at minimum bearerToken/oauth/unsupported must be distinguished from notLoggedIn.)\n3) In RequestRouter.swift mcpStatusJSON (line 3959), replace .string(\"notLoggedIn\") with .string(status.authStatus) computed from the per-server config above. Pass codexHome/store into mcpStatusListData so the helper can read stored OAuth tokens. Ensure the camelCase enum values used are unsupported/notLoggedIn/bearerToken/oauth to match app-server v2 McpAuthStatus serialization.

[mcp] tools/call cannot send request-level _meta over the wire (wrong-semantics)
  RUST: rmcp-client/src/rmcp_client.rs:545-608 call_tool accepts a `meta` arg and sends it as JSON-RPC `_meta` via send_request_with_option(PeerRequestOptions{ meta }); core/src/mcp_tool_call.rs:559-581 builds request_meta with thread_id (with_mcp_tool_call_thread_id_meta) and sandbox state (augment_mcp_tool_request_meta_with_sandbox_state) keyed on the server's codex/sandbox-state-meta capability (rmcp_client.rs:69) and passes it to call_tool.
  SWIFT: Sources/MCP/McpClient.swift:688-699 and McpHttpClient.swift:690-708 callTool build params with only `name` and `arguments`; the tools/call request never includes a top-level `_meta`. McpClientProtocol.callTool has no meta parameter. McpClient.swift:698 sends `["name": name, "arguments": argsObject]` exclusively.
  DESC: Upstream attaches request-level _meta to tools/call (conversation/thread id, and SandboxState when the server advertises the codex/sandbox-state-meta capability, plus approval metadata). Swift's MCP client API has no path to send _meta at all, so MCP servers relying on thread-id or sandbox-state meta receive none of it. The server_supports_sandbox_state_meta_capability negotiation is also absent.
  FIX: 1) Extend the surface: add `meta: JSONLite?` (or `[String: JSONLite]?`) param to `McpClientProtocol.callTool` (Sources/MCP/McpClient.swift:340), keeping the existing 2-arg convenience overload at line 346-350 defaulting meta=nil. 2) Stdio impl (McpClient.swift:688-699): when meta != nil, include it as `params["_meta"] = metaObject` alongside name/arguments before calling `request("tools/call", ...)`. 3) HTTP impl (McpHttpClient.swift:690-708): same — add `"_meta": metaObject` to the params dict. 4) Negotiate capability: in `initialize()` (McpClient.swift:664-672 and the HTTP equivalent) capture the server's `capabilities` from the initialize result and store a bool like `serverSupportsSandboxStateMeta = capabilities["codex/sandbox-state-meta"] != nil` on the client actor; expose it via the protocol/McpManager. 5) Build meta at the call site: in McpManager.callTool (McpManager.swift:356-364) accept/construct request meta and forward it. Insert key "threadId" = conversation id unconditionally, and when the server advertises the capability insert key "codex/sandbox-state-meta" = the encoded SandboxState (permission_profile, sandbox_policy, sandbox_cwd, etc.), mirroring with_mcp_tool_call_thread_id_meta and augment_mcp_tool_request_meta_with_sandbox_state. 6) Thread the conversation id and turn/sandbox context from the tool-execution path (the equivalent of execute_mcp_tool_call) down to McpManager.callTool so the meta can be assembled. Use exact upstream key strings: "threadId" and "codex/sandbox-state-meta".

[auth] account/rateLimits/updated not emitted during turns; emitted spuriously on read (missing-notification)
  RUST: app-server/src/bespoke_event_handling.rs:1571-1579 emits ServerNotification::AccountRateLimitsUpdated from the TokenCount/rate_limits event during a turn; account_processor.rs:833-849 get_account_rate_limits_response returns ONLY the response and emits no notification.
  SWIFT: Sources/Supervisor/RequestRouter.swift:1768-1769 emits ServerNotification.accountRateLimitsUpdated inside the account/rateLimits/read handler; grep shows accountRateLimitsUpdated is referenced nowhere else (no turn/token-count emission).
  DESC: Upstream pushes account/rateLimits/updated whenever a turn's API response carries rate-limit headers (via the token-count event). Swift never emits it during turns and instead emits it as a side effect of the account/rateLimits/read request. A frontend that relies on live rate-limit push updates during a turn gets none, and gets an unexpected notification on every read.
  FIX: Two coordinated changes:

1. Emit account/rateLimits/updated during turns. The snapshot recorded by tracker.recordRateLimits() must be surfaced to the engine and emitted. Concretely: after a model API response that carried rate-limit headers, when SessionEngine emits the per-turn `.tokenUsageUpdated` (Sources/HarnessCore/SessionEngine.swift around line 1768, and the streaming/compaction emission sites ~1008 and ~1394), also emit `.accountRateLimitsUpdated(rateLimits: <snapshot JSON>)` whenever a non-nil rate-limit snapshot is available. This requires threading the parsed RateLimitSnapshot from the ModelClient stream out to SessionEngine (e.g. surface it via a stream event / the UsageSnapshot path, or read it from the provider's lastRateLimits()) and converting it to the same JSON shape used by RequestRouter (the `rateLimits` object produced around RequestRouter.swift:1189-1204). Mirror upstream handle_token_count_event: only emit when rate_limits is present.

2. Remove the spurious emission on read. In Sources/Supervisor/RequestRouter.swift, delete the post-reply `await conn.send(ServerNotification.accountRateLimitsUpdated(rateLimits: snapshot).toMessage())` at lines 1768-1769 in the getAccountRateLimits handler, leaving only the `await reply(conn, id, result)`. Upstream's get_account_rate_limits_response returns the response with no notification.

[auth] RateLimitSnapshot.credits always null instead of mapped from backend payload (wrong-field)
  RUST: backend-client/src/client.rs:455-501 maps payload.credits into CreditsSnapshot via map_credits (client.rs:557-565: has_credits, unlimited, balance); v2/account.rs:358-372 CreditsSnapshot{has_credits,unlimited,balance}.
  SWIFT: Sources/Supervisor/RequestRouter.swift:1219 hardcodes "credits": .null in rateLimitSnapshot(); body["credits"] is never read in rateLimitResponse (lines 1180-1206).
  DESC: The V2 RateLimitSnapshot.credits object (hasCredits/unlimited/balance) is populated upstream from the backend's `credits` field on the top-level (codex) snapshot. Swift always emits null, so a frontend cannot read credit balance / unlimited state from account/rateLimits/read or the updated notification.
  FIX: In Sources/Supervisor/RequestRouter.swift:

1) Add a credits parameter to rateLimitSnapshot and map it. Replace the hardcoded line 1219 ("credits": .null) by computing from a passed-in JSONValue?:

   private static func rateLimitSnapshot(limitId: String,
                                         limitName: String?,
                                         rateLimit: JSONValue?,
                                         credits: JSONValue?,
                                         planType: String?,
                                         reachedType: JSONValue?) -> JSONValue {
       ...
       "credits": mapCredits(credits),
       ...
   }

   private static func mapCredits(_ value: JSONValue?) -> JSONValue {
       guard let obj = value?.objectValue else { return .null }
       let hasCredits = obj["has_credits"]?.boolValue ?? false
       let unlimited = obj["unlimited"]?.boolValue ?? false
       let balance = obj["balance"]?.stringValue
       return .object([
           "hasCredits": .bool(hasCredits),
           "unlimited": .bool(unlimited),
           "balance": balance.map(JSONValue.string) ?? .null,
       ])
   }

2) In rateLimitResponse (lines 1182-1200): for the primary "codex" snapshot pass credits: body["credits"]; for the additional_rate_limits loop pass credits: nil (matching upstream which uses None for additional limits).

This reproduces map_credits + the codex-only placement exactly.

[auth] account/read fabricates empty email instead of returning invalid_request when ChatGPT email missing (wrong-semantics)
  RUST: model-provider/src/provider.rs:216-224 requires (Some(email), Some(plan_type)) else returns ProviderAccountError::MissingChatgptAccountDetails; account_processor.rs:818-824 converts that to invalid_request("email and plan type are required for chatgpt authentication").
  SWIFT: Sources/Supervisor/RequestRouter.swift:3634-3648 accountJSON returns chatgpt with email = claims?.email ?? "" and planType = claims?.planType ?? "unknown"; getAccountResponse (3593-3595) returns this without ever erroring.
  DESC: When the stored ChatGPT id_token has no email claim, upstream fails the account/read request with a specific invalid_request error. Swift instead returns a successful chatgpt account with an empty-string email, which violates the contract a frontend may rely on (it would render an account with a blank email rather than prompting re-auth).
  FIX: Make account/read error with invalid_request when a chatgpt (bearer/bearerexternal) token lacks email or plan_type, matching upstream's (Some,Some)-or-error contract.

1) In RequestRouter.swift, change accountJSON(for:) to surface the missing-details case for chatgpt tokens instead of substituting defaults. E.g. introduce a small result type:
   private enum AccountResult { case json(JSONValue?); case missingChatgptDetails }
   and in the "bearer"/"bearerexternal" branch:
     let claims = tokenClaims(tokens.idToken) ?? tokenClaims(tokens.accessToken)
     guard let email = claims?.email, let plan = claims?.planType else { return .missingChatgptDetails }
     return .json(.object(["type": .string("chatgpt"), "email": .string(email), "planType": .string(plan)]))
   (Keep apiKey and default branches returning .json as today.)

2) Make getAccountResponse(refreshToken:) throwing (or return an enum), so it can propagate the missing-details condition. When accountJSON yields .missingChatgptDetails, throw/return a sentinel.

3) In the dispatch at RequestRouter.swift:1744-1746, replace the unconditional reply with the same pattern used by the adjacent .getAccountRateLimits case: on missing-details, send
   await conn.send(WireError.invalidRequest(id: id, "email and plan type are required for chatgpt authentication"))
   and return; otherwise reply with the GetAccountResponse as before.

Use the exact upstream message string ("email and plan type are required for chatgpt authentication") to preserve fidelity.

======================================================================
MINOR
======================================================================

[protocol-wire-types] ServerNotification emits non-existent method strings item/plan/updated, item/requestUserInput, item/requestPermissions (wrong-field)
  RUST: Upstream plan notification is `TurnPlanUpdated => "turn/plan/updated"` (common.rs:1469) with payload `TurnPlanUpdatedNotification` and step status enum `TurnPlanStepStatus` rename_all=camelCase → `pending|inProgress|completed` (turn.rs:367-389). There is NO `item/plan/updated`. `requestUserInput`/`requestPermissions` exist only as server REQUESTS `item/tool/requestUserInput` (common.rs:1318) and `item/permissions/requestApproval` (common.rs:1330), not notifications.
  SWIFT: Sources/ProtocolModel/Events.swift:222-224 `.planUpdate -> "item/plan/updated"`, `.requestUserInput -> "item/requestUserInput"`, `.requestPermissions -> "item/requestPermissions"`; `.planUpdate` uses `PlanStepStatus` with snake_case `in_progress` (Events.swift:494-498). These cases are never emitted (PlanUpdateBus published-to but never subscribed; no emit of the request cases found), so currently dead.
  DESC: If/when wired these would send method strings and (for plan) a snake_case status value that no upstream client recognizes. The correct path is the server-request route (already correctly implemented in ServerRequest.swift) and a `turn/plan/updated` notification with camelCase `inProgress`.
  FIX: Either remove the dead `.requestUserInput`/`.requestPermissions` notification cases (the real flow goes through ServerRequest.swift), and rename `.planUpdate`'s method to `turn/plan/updated` with a camelCase step-status enum, or delete `.planUpdate` if plan streaming is intentionally unimplemented.

[protocol-wire-types] ErrorNotification threadId/turnId modeled optional and omitted; codexErrorInfo typed as String (wrong-field)
  RUST: app-server-protocol/src/protocol/v2/notification.rs `ErrorNotification { error: TurnError, will_retry: bool, thread_id: String, turn_id: String }` — thread_id/turn_id required. TurnError (thread_data.rs:191-196) `{ message, codex_error_info: Option<CodexErrorInfo>, additional_details: Option<String> }` where codex_error_info is a structured type.
  SWIFT: Sources/ProtocolModel/Events.swift:432-437 `struct ErrorNotification { error, willRetry, threadId: ThreadId?, turnId: TurnId? }` (optional, omitted when nil); ErrorBody.codexErrorInfo typed `String?` (Events.swift:5).
  DESC: Upstream `error` notifications always include `threadId` and `turnId`; Swift omits them when nil. `codexErrorInfo` is a structured object upstream but a String in Swift. Low severity because clients generally tolerate missing thread/turn ids and codexErrorInfo is usually null, but it diverges from the documented required shape.
  FIX: Always emit `threadId`/`turnId` for the `error` notification (use empty/known values rather than omitting), and model `codexErrorInfo` as a structured JSONValue/typed object rather than String if the field is ever populated.

[app-server-registry] Unknown (non-Codex) method returns -32601 instead of upstream's -32600 invalid_request (missing-error-code)
  RUST: message_processor.rs:536-541: a request whose method does not deserialize into the tagged ClientRequest enum is mapped via invalid_request(format!("Invalid request: {err}")) = code -32600 (error_code.rs:3). method_not_found (-32601) is used upstream only for specific known-but-unimplemented operations (thread_processor.rs:623, :3587, :3713), never for a genuinely unknown method.
  SWIFT: ClientRequest.swift:429-434 routes methods not in Method.all to .unsupported, dispatched at RequestRouter.swift:2069-2070 via WireError.unsupported -> code -32601 'X is not supported yet' (WireErrors.swift:8,23-25,30-32).
  DESC: For a truly unknown method string, upstream answers -32600 'Invalid request: ...' but Swift answers -32601 'X is not supported yet'. Both the code and message text differ from the upstream contract.
  FIX: Change the .unsupported path in RequestRouter.swift:2069 to emit invalid_request (-32600) with a message of the form 'Invalid request: ...' to match message_processor.rs:540, and reserve -32601 only for the upstream known-but-unsupported cases.

[app-server-registry] thread/turns/items/list returns data where upstream returns -32601 'not supported yet' (wrong-semantics)
  RUST: request_processors/thread_processor.rs:619-626 thread_turns_items_list returns Err(method_not_found('thread/turns/items/list is not supported yet')) (-32601); asserted in tests/suite/v2/thread_read.rs:1090-1093.
  SWIFT: RequestRouter.swift:1548-1552 threadTurnsItemsList returns a real {data, nextCursor, backwardsCursor} success response from store.turnItems.
  DESC: Upstream deliberately returns -32601 'thread/turns/items/list is not supported yet' for this method; Swift implements it and returns data. A client coded against the documented -32601 contract sees a success response instead.
  FIX: If strict parity is required, make RequestRouter.swift threadTurnsItemsList return WireError with code -32601 and message 'thread/turns/items/list is not supported yet'. Otherwise document this as an intentional superset behavior.

[app-server-events] item/plan/delta notification missing (missing-notification)
  RUST: app-server-protocol/src/protocol/common.rs:1478 `PlanDelta => "item/plan/delta"`; payload item.rs:1167-1172 {threadId,turnId,itemId,delta}; mapped event_mapping.rs:355-360 from EventMsg::PlanDelta.
  SWIFT: ABSENT. No item/plan/delta case/method in Sources/ProtocolModel/Events.swift; grep across Sources/ returns nothing.
  DESC: Experimental plan-item streaming deltas (item/plan/delta) are not reproduced. Marked EXPERIMENTAL upstream and low-traffic, but a client subscribed to it gets nothing.
  FIX: Add a `.planDelta(threadId,turnId,itemId,delta)` case (method item/plan/delta) if plan-mode streaming is in scope; otherwise document as intentionally unsupported.

[app-server-events] rawResponseItem/completed notification missing (missing-notification)
  RUST: app-server-protocol/src/protocol/common.rs:1474-1475 `RawResponseItemCompleted => "rawResponseItem/completed"`; payload item.rs:1145-1149 {threadId,turnId,item: ResponseItem}; emitted bespoke_event_handling.rs:1386-1399 maybe_emit_raw_response_item_completed on EventMsg::RawResponseItem.
  SWIFT: ABSENT. No rawResponseItem case/method in Sources/ (grep returns nothing).
  DESC: Upstream forwards each raw ResponseItem via rawResponseItem/completed (documented internal-only, used by Codex Cloud). Swift omits it. Low priority for a local frontend but a fidelity gap.
  FIX: If Codex-Cloud-style consumers are out of scope, document as intentionally unsupported; otherwise add the case and emit from the raw-item path.

[app-server-events] item/autoApprovalReview/started and /completed notifications missing (missing-notification)
  RUST: app-server-protocol/src/protocol/common.rs:1471-1472 ItemGuardianApprovalReviewStarted => "item/autoApprovalReview/started", ItemGuardianApprovalReviewCompleted => "item/autoApprovalReview/completed". Payloads item.rs:1073-1128 (review_id, target_item_id, review: GuardianApprovalReview, action, decision_source, started_at_ms/completed_at_ms). Tested bespoke_event_handling.rs:2332-2440.
  SWIFT: ABSENT. No autoApprovalReview / GuardianApprovalReview case anywhere in Sources/ (grep returns nothing).
  DESC: Upstream's guardian auto-approval review lifecycle (item/autoApprovalReview/started + completed) is not reproduced. Marked [UNSTABLE] upstream so low priority, but absent entirely.
  FIX: If guardian auto-review is out of scope, document; otherwise add the two notification cases with the GuardianApprovalReview/Action payloads.

[app-server-events] item/commandExecution/outputDelta injects '[stderr] ' prefix into delta payload (wrong-semantics)
  RUST: app-server-protocol/src/protocol/event_mapping.rs:419-430: delta = `String::from_utf8_lossy(&exec_command_output_delta_event.chunk).to_string()` — the raw chunk bytes only, no stream label. (CommandExecutionOutputDeltaNotification carries no stream field.)
  SWIFT: Sources/HarnessCore/SessionEngine.swift:1332-1338: `delta: stream == "stderr" ? "[stderr] " + s : s` — prepends a literal "[stderr] " marker to stderr chunks.
  DESC: Upstream emits the raw decoded chunk as the delta with no stream prefix; stdout and stderr are interleaved verbatim. Swift mutates stderr deltas by prepending '[stderr] ', so concatenated client-side output will not match the real terminal stream and will contain spurious markers.
  FIX: In SessionEngine, emit the decoded chunk verbatim for both streams (drop the '[stderr] ' prefix) to match upstream from_utf8_lossy behavior.

[session-turn-loop] Plan notification `explanation` omitted when nil; upstream always serializes it (including null) (wrong-field)
  RUST: app-server-protocol/src/protocol/v2/turn.rs:367-372 `pub explanation: Option<String>` has NO `#[serde(skip_serializing_if)]`, so serde emits `"explanation": null` when absent.
  SWIFT: Sources/ProtocolModel/Events.swift:449-454 PlanUpdated.encode uses `encodeIfPresent(explanation)`, omitting the key entirely when nil (the doc comment at 439-440 incorrectly claims upstream skips it).
  DESC: Upstream always includes the `explanation` key (as null when there is none); the Swift port omits the key. Clients that treat a missing key differently from an explicit null could diverge. Low traffic / cosmetic but a literal shape difference.
  FIX: In Events.swift PlanUpdated.encode, encode `explanation` unconditionally (`try c.encode(explanation, forKey: .explanation)`), letting nil serialize as JSON null.

[context-compaction] Model-downshift pre-turn compaction path and CompactionReason.model_downshift are absent (missing-notification)
  RUST: turn.rs:772-813 maybe_run_previous_model_inline_compact triggers a pre-turn compaction (CompactionReason::ModelDownshift, CompactionPhase::PreTurn) when total_usage_tokens > new_auto_compact_limit AND the previous model slug differs AND old_context_window > new_context_window. CompactionReason::ModelDownshift is a tracked analytics signal.
  SWIFT: SessionEngine.swift:858-862 CompactionReason enum has only contextLimit and userRequested; there is no model_downshift case and no maybe_run_previous_model_inline_compact equivalent (run_pre_sampling_compact maps only to the single threshold check at SessionEngine.swift:1071).
  DESC: When a session switches to a smaller-context model, upstream pre-emptively compacts against the previous model before the first turn on the new model. Swift has neither the trigger nor the model_downshift reason. CompactionReason feeds analytics (not the frontend JSON-RPC protocol), so impact is limited to a missing edge-case behavior and analytics signal rather than wire breakage.
  FIX: If the Swift port supports mid-session model switching, add a previous-model pre-turn compaction check in the pre-sampling path of SessionEngine and add a `modelDownshift = "model_downshift"` case to CompactionReason. Otherwise document the omission.

[context-compaction] Boundary-message truncation marker text differs from upstream token-truncation marker (prompt-text)
  RUST: compact.rs:497 truncate_text(message, TruncationPolicy::Tokens(remaining)) → utils/string/src/truncate.rs:131-137 emits marker `…{N} tokens truncated…` (middle truncation) for the user message that overflows the remaining COMPACT_USER_MESSAGE_MAX_TOKENS budget.
  SWIFT: Compaction.swift:56-62 truncateToTokens uses InfraPrimitives HeadTailBuffer; Sources/InfraPrimitives/Rings.swift:182 renders the marker as `… {N} bytes elided …` (with surrounding newlines).
  DESC: When a single user message exceeds the remaining token budget during buildCompactedHistory, the truncated message embedded in the compacted (model-visible) history carries a different elision marker in Swift (`… N bytes elided …`) than upstream (`…N tokens truncated…`). This is model-facing summary content, not a wire-protocol field, and only triggers for an oversized boundary message, so impact is low.
  FIX: If exact model-visible parity is desired for compacted history, make the compaction-path truncation emit the upstream `…{tokens} tokens truncated…` middle-truncation marker instead of HeadTailBuffer's byte-elided marker (or reuse a shared truncate_text-equivalent that matches truncate.rs).

[model-client] Rate-limit parsing only reads the single "codex" family; upstream discovers all limit families (wrong-semantics)
  RUST: codex-api/src/rate_limits.rs:27-50 parse_all_rate_limits scans every x-<limit>-primary-used-percent header (header_name_to_limit_id, lines 249-254) and emits one RateLimitSnapshot per family (e.g. codex, codex_secondary, codex_bengalfox), in addition to the default codex snapshot. sse/responses.rs:35,68-69 feeds each into ResponseEvent::RateLimits.
  SWIFT: Sources/ModelClient/ModelProvider.swift:349-386 parseRateLimits is hardcoded to limitId="codex" and returns a single snapshot; callers (OpenAIResponsesClient.swift:306, URLSessionResponsesClient.swift:281-286) only ever parse the codex family. No multi-family discovery; resetAt is stored as String (line 321) vs upstream i64 (rate_limits.rs:101-105).
  DESC: Upstream surfaces every metered rate-limit family the server returns; Swift only ever reports the default codex family, so secondary/other limit windows shown by the frontend are missing.
  FIX: Port parse_all_rate_limits: enumerate header names ending in -primary-used-percent, derive each limit_id, and parse a snapshot per family; also parse reset-at as an integer to match RateLimitWindow.resets_at (i64).

[model-client] text.format (JSON-schema output / codex_output_schema) is never emitted on the request (missing-type)
  RUST: codex-api/src/common.rs:279-297 create_text_param_for_request emits TextControls.format = TextFormat{type:json_schema, strict, schema, name:"codex_output_schema"} whenever output_schema is set; client.rs:738-742 wires prompt.output_schema/output_schema_strict into it. client_common_tests.rs:45-117 asserts the exact format.name/type/strict/schema wire shape.
  SWIFT: Partially ABSENT. OpenAIResponsesClient.swift:249-251 emits only text:{verbosity:...}; never text.format. Swift Prompt (ModelClient.swift:41-54) has no output_schema/output_schema_strict fields, and the comment at line 247-248 explicitly defers it.
  DESC: Upstream supports prompt-level structured-output enforcement via text.format with the codex_output_schema name. Swift cannot request structured JSON-schema output through text.format at all.
  FIX: Add output_schema/output_schema_strict to Prompt (or ModelSettings) and emit text.format = {type:"json_schema", strict, schema, name:"codex_output_schema"} in buildRequestBody, matching create_text_param_for_request.

[model-client] response.created emitted without requiring a response payload (wrong-semantics)
  RUST: codex-api/src/sse/responses.rs:307-311: "response.created" only yields ResponseEvent::Created when event.response.is_some().
  SWIFT: OpenAIResponsesClient.swift:411-412 (and URLSessionResponsesClient.swift:201-202, WebSocketResponsesClient.swift:182-183) yield .created unconditionally on the type match without checking for a `response` object.
  DESC: Minor semantic divergence: Swift emits .created even for a malformed response.created frame lacking the response object, where upstream suppresses it. Low impact since well-formed frames always include response.
  FIX: Guard the .created yield on the presence of obj["response"] to mirror upstream.

[tools-router] tool_search exposed as function tool instead of type:"tool_search" with execution:"client" (wrong-semantics)
  RUST: core/src/tools/handlers/tool_search_spec.rs:53-61 returns ToolSpec::ToolSearch{ execution:"client", description, parameters }; tools/src/tool_spec.rs:22-27 serializes with #[serde(rename="tool_search")] producing {"type":"tool_search","execution":"client",...}. router.rs:93-110 dispatches ResponseItem::ToolSearchCall (execution=="client").
  SWIFT: Sources/Tools/ToolRouter.swift:354-361 ToolSearchTool name="tool_search" with a plain object schema; serialized as type:"function" (OpenAIResponsesClient.swift:191). No tool_search_call parsing exists in the client.
  DESC: Upstream emits the tool_search tool with wire type tool_search and execution=client, and consumes tool_search_call items. Swift emits a normal function tool and consumes function_call. Internally consistent for Swift's harness, but the request sent to OpenAI and the expected response item type differ from upstream.
  FIX: If exact OpenAI request parity is required, serialize tool_search as {"type":"tool_search","execution":"client","description":...,"parameters":...} and parse tool_search_call response items; otherwise note as an intentional divergence.

[tools-router] Tool definitions omit the always-present strict:false field (wrong-field)
  RUST: tools/src/responses_api.rs:26-38 ResponsesApiTool has pub strict: bool with no skip attribute, so every function tool serializes "strict":false. tool_spec.rs:18-19 Function(ResponsesApiTool) under #[serde(tag="type")].
  SWIFT: Sources/ModelClient/OpenAIResponsesClient.swift:191-193 builds entry = ["type":"function","name":...,"description":...,"parameters":...] with no strict key; grep for "strict" across Sources/ModelClient and Sources/Tools shows no tool-definition usage.
  DESC: Upstream always includes strict:false in each function tool object; Swift never emits it. Behaviorally low-impact (false is the effective default) but the serialized tool object differs field-for-field from upstream.
  FIX: Add "strict": false to the tool entry dictionary in OpenAIResponsesClient.swift (and the WebSocket/URLSession clients) when serializing function tools.

[tools-router] Tool definitions emit output_schema, which upstream explicitly never sends on the wire (wrong-field)
  RUST: tools/src/responses_api.rs:36-37 #[serde(skip)] pub output_schema: Option<Value> — output_schema is intentionally excluded from the serialized Responses-API tool object even when present (it is used internally only).
  SWIFT: Sources/ModelClient/OpenAIResponsesClient.swift:198-201 adds entry["output_schema"] = parsed whenever spec.outputSchemaJSON is non-nil; ExecCommandTool/WriteStdinTool/ViewImageTool can declare outputSchemaJSON.
  DESC: Swift adds an output_schema field to the serialized tool definition that upstream deliberately strips (#[serde(skip)]). This sends a field to OpenAI that the upstream harness never sends, diverging from the wire contract.
  FIX: Remove the output_schema emission in the tool serialization path (OpenAIResponsesClient.swift:198-201) to match upstream's #[serde(skip)]; keep output_schema as an internal-only field if used elsewhere.

[tools-router] exec_command marked serial in Swift but parallel-safe upstream (wrong-semantics)
  RUST: core/src/tools/handlers/unified_exec/exec_command.rs supports_parallel_tool_calls() returns true (confirmed via grep: 'fn supports_parallel_tool_calls -> true').
  SWIFT: Sources/Tools/UnifiedExec.swift:820 ExecCommandTool public let parallelSafe = false.
  DESC: Upstream's exec_command participates in parallel tool calls (read side of the RwLock gate); Swift forces it serial (write side). This is internal turn-gating concurrency only (no wire field), so impact is limited to throughput/ordering, not protocol fidelity.
  FIX: Set ExecCommandTool.parallelSafe = true to match unified_exec/exec_command.rs if exact gating parity is desired; minor.

[tools-router] wait_agent marked parallel-safe in Swift but serial upstream (wrong-semantics)
  RUST: core/src/tools/handlers/multi_agents_common.rs has no supports_parallel_tool_calls override (grep count 0), so WaitAgentHandler inherits the trait default false (tools/src/tool_executor.rs:50-52 fn supports_parallel_tool_calls -> false).
  SWIFT: Sources/Tools/MultiAgentTools.swift:259 WaitAgentTool parallelSafe = true (and Sources/HarnessCore/MultiAgent.swift:325 wait tool parallelSafe = true).
  DESC: All upstream multi-agent handlers (spawn/wait/close/send/resume) use the default serial gating; Swift marks wait_agent parallel-safe, allowing it to run concurrently with other tools under the read side of the gate. Internal gating only, no wire effect.
  FIX: Set WaitAgentTool.parallelSafe = false (both MultiAgentTools.swift and HarnessCore/MultiAgent.swift) to match upstream's default-serial multi-agent handlers; minor.

[exec-unified-shell] exec_command JSON omits chunk_id field declared in the output schema (wrong-field)
  RUST: core/src/tools/handlers/shell_spec.rs:247-279 unified_exec_output_schema declares a `chunk_id` string property; core/src/tools/context.rs:367/379 code_mode_result emits `chunk_id` when non-empty; process_manager.rs:461/581/668/725 always generates a chunk_id via generate_chunk_id().
  SWIFT: Sources/Tools/UnifiedExec.swift:784-809 renderExecJSON never produces a `chunk_id` key (no chunk-id is generated at all in the local exec_command/write_stdin path).
  DESC: Upstream attaches a freshly generated 6-hex-char chunk_id to each exec_command/write_stdin result and surfaces it (Chunk ID line in text, chunk_id field in JSON). The Swift port generates no chunk id and never emits the field, so any client/model relying on chunk_id continuity loses it.
  FIX: Generate a 6 hex-digit chunk id per call (matching generate_chunk_id in unified_exec/mod.rs:169-174) and include it in the output (as a `Chunk ID:` text section if adopting the plain-text format, or a `chunk_id` JSON field for code-mode).

[exec-unified-shell] exec_command schema is missing upstream `shell`, `tty`, and approval parameters and adds non-upstream `timeout_ms` (wrong-semantics)
  RUST: core/src/tools/handlers/shell_spec.rs:23-100 create_exec_command_tool defines properties: cmd, workdir, shell, tty, yield_time_ms, max_output_tokens (+ optional login, environment_id, and approval params sandbox_permissions/justification/prefix_rule/additional_permissions). Required = [cmd]. No `timeout_ms`.
  SWIFT: Sources/Tools/UnifiedExec.swift:830-832 jsonSchema declares only {cmd, cwd, timeout_ms, yield_time_ms, max_output_tokens}; ExecCommandTool.Args (UnifiedExec.swift:866-891) parses cmd/cwd(+workdir alias)/timeout_ms/yield_time_ms/max_output_tokens — no shell, tty, login, or approval fields.
  DESC: Upstream's exec_command exposes `shell` (choose shell binary), `tty` (PTY vs pipes, default false), and the approval triad (sandbox_permissions/justification/prefix_rule). Swift drops all of these and instead invents a `timeout_ms` alias and uses `cwd` as the primary name (upstream's canonical name is `workdir`; Swift accepts it only as an alias). A model trained on the upstream schema cannot request a specific shell, request non-TTY pipes, or escalate permissions through this tool.
  FIX: Align ExecCommandTool's schema/args with shell_spec.rs: use `workdir` as the canonical key, add `shell`, `tty` (default false), and the approval params; drop `timeout_ms` (not an upstream field).

[exec-unified-shell] exec_command sessions are always PTY; upstream defaults tty=false and rejects non-empty write_stdin on non-TTY sessions (StdinClosed) (wrong-semantics)
  RUST: core/src/tools/handlers/unified_exec.rs:37-38 `default_tty() -> false`. core/src/unified_exec/process_manager.rs:617-620 write_stdin: `if !request.input.is_empty() { if !tty { return Err(UnifiedExecError::StdinClosed); } ... }`. errors.rs:15-18 StdinClosed message: "stdin is closed for this session; rerun exec_command with tty=true to keep stdin open".
  SWIFT: Sources/Tools/UnifiedExec.swift PTY.spawn (312-402) always allocates a PTY; UnifiedExecManager.open/writeStdin never models a non-TTY/pipe mode; write_stdin never returns a StdinClosed-equivalent error.
  DESC: Upstream opens commands with plain pipes by default (tty=false) and refuses non-empty write_stdin unless the session was opened with tty=true, surfacing the specific StdinClosed message. Swift always opens a PTY and always accepts writes, so the default interaction model and the StdinClosed error contract are absent.
  FIX: Add a tty flag (default false) to exec_command, spawn pipes when false, and have write_stdin return the StdinClosed message verbatim when input is non-empty on a non-TTY session.

[exec-unified-shell] exec_command / write_stdin tool descriptions are rewritten, not the upstream verbatim strings (prompt-text)
  RUST: core/src/tools/handlers/shell_spec.rs:88-90 exec_command description (non-Windows): "Runs a command in a PTY, returning output or a session ID for ongoing interaction." shell_spec.rs:133-135 write_stdin description: "Writes characters to an existing unified exec session and returns recent output."
  SWIFT: Sources/Tools/UnifiedExec.swift:821-829 ExecCommandTool.toolDescription is a multi-sentence rewrite ("Open a NEW interactive PTY session and run a shell command. Returns a structured JSON object ..."); UnifiedExec.swift:971-976 WriteStdinTool.toolDescription is also rewritten.
  DESC: The model-facing tool descriptions differ from the upstream verbatim text. Since these strings are part of the prompt surface the model is trained against, the rewrites change tool selection behavior.
  FIX: Set ExecCommandTool.toolDescription to the exact upstream string and WriteStdinTool.toolDescription to its exact upstream string from shell_spec.rs.

[exec-unified-shell] write_stdin empty-poll yield capped at 30s; upstream caps at the configurable background timeout (default 300s) (wrong-default)
  RUST: core/src/unified_exec/process_manager.rs:642-651: empty input -> `time_ms.clamp(MIN_EMPTY_YIELD_TIME_MS, self.max_write_stdin_yield_time_ms)` where max defaults to DEFAULT_MAX_BACKGROUND_TERMINAL_TIMEOUT_MS = 300_000 (mod.rs:66, 146).
  SWIFT: Sources/Tools/UnifiedExec.swift:1025-1027 (WriteStdinTool) and 688-691 (UnifiedExecTool) empty path: `Swift.max(MIN_EMPTY_YIELD_TIME_MS, uxClamp(requested, YIELD_MIN_MS, YIELD_MAX_MS))` where YIELD_MAX_MS = 30_000.
  DESC: For empty (poll) write_stdin calls upstream allows a long-poll up to 300s (configurable), while Swift caps the upper bound at 30s. A model issuing an empty poll with a large yield_time_ms gets a much shorter wait than upstream.
  FIX: For the empty-input branch, clamp to [MIN_EMPTY_YIELD_TIME_MS (5000), DEFAULT_MAX_BACKGROUND_TERMINAL_TIMEOUT_MS (300000)] instead of YIELD_MAX_MS, mirroring process_manager.rs:647.

[apply-patch] Context-not-found and expected-lines error text not reproduced (prompt-text)
  RUST: apply-patch/src/lib.rs:714-718 `"Failed to find context '{ctx}' in {path}"` and lib.rs:771-775 `"Failed to find expected lines in {path}:\n{old_lines joined}"` (ComputeReplacements error surfaced to model via apply_hunks stderr at lib.rs:335-336).
  SWIFT: Sources/Tools/ApplyPatch.swift:517 and :535 both throw `ApplyPatchError.contextMismatch("\(path): hunk context not found")` for both failure modes; surfaced as `"apply_patch failed: ..."` in ToolRouter.swift:414.
  DESC: Both upstream failure cases (missing @@ context line vs missing old_lines body) produce distinct, content-rich messages naming the context string or the expected lines; Swift collapses both into a single generic message lacking the offending text. The model relies on these to self-correct patches.
  FIX: Use distinct messages in deriveNewContents: for missing context emit "Failed to find context '<ctx>' in <path>"; for missing old_lines emit "Failed to find expected lines in <path>:\n<oldLines joined by \n>".

[apply-patch] Invalid-hunk-header error text differs from upstream verbatim message (prompt-text)
  RUST: apply-patch/src/parser.rs:368-373 — `"'{first_line}' is not a valid hunk header. Valid hunk headers: '*** Add File: {path}', '*** Delete File: {path}', '*** Update File: {path}'"` (asserted in test_parse_one_hunk line 472-474).
  SWIFT: Sources/Tools/ApplyPatch.swift:344 throws `ApplyPatchError.malformed("unexpected line: \(line)")` for an unrecognized hunk header.
  DESC: Upstream emits a precise, model-actionable header-format hint listing the three valid hunk headers; Swift emits a terse generic message. This is model-facing guidance text that upstream treats as load-bearing.
  FIX: Replace the generic message with upstream's verbatim text: "'<line>' is not a valid hunk header. Valid hunk headers: '*** Add File: {path}', '*** Delete File: {path}', '*** Update File: {path}'".

[apply-patch] Success summary output format differs from upstream print_summary (wrong-semantics)
  RUST: apply-patch/src/lib.rs:855-865 print_summary writes `"Success. Updated the following files:\n"` then per path `"A {path}"`, `"M {path}"`, `"D {path}"` (added, then modified, then deleted; tests at lib.rs:908-911, 981-983).
  SWIFT: Sources/Tools/ToolRouter.swift:410-411 builds `applied.map { "\($0.kind.rawValue) \($0.path)" }` joined by \n with prefix `"applied:\n"`; kind.rawValue is lowercase "add"/"update"/"delete" (ApplyPatch.swift:12).
  DESC: The tool result string returned to the model on success differs from upstream: upstream uses the header 'Success. Updated the following files:' with single-letter A/M/D markers grouped added→modified→deleted; Swift emits 'applied:' with lowercase full words in patch order. A frontend or model expecting the upstream summary format will not match.
  FIX: In ApplyPatchTool.run (and RemoteExecServerApplyPatchTool), emit upstream's format: header line 'Success. Updated the following files:' followed by 'A '/'M '/'D ' + path, grouped added, then modified, then deleted.

[persistence-rollout] `compacted` rollout payload carries non-upstream `turn_id` field (wrong-field)
  RUST: codex-rs/protocol/src/protocol.rs:2793-2798 `CompactedItem { message: String, replacement_history: Option<Vec<ResponseItem>> }` — no `turn_id` field exists.
  SWIFT: Sources/Persistence/Rollout.swift:546-559 writes `payload = ["turn_id": tid.raw, "message": summary]` (+ replacement_history). The `turn_id` key is not part of upstream CompactedItem.
  DESC: Upstream CompactedItem has no turn_id; serde will ignore the unknown field on read so it does not break deserialization, but the on-disk shape is not byte-faithful and Swift's own reader (Rollout.swift:846) depends on the extra field for turn association, which an upstream-written compacted line (lacking turn_id) handles only via the `currentTurnId` fallback.
  FIX: Optional: keep turn_id for Swift-internal association but be aware it is non-canonical; if strict byte-fidelity is desired, drop `turn_id` from the compacted payload and rely on the surrounding turn cursor for association as the reader already does.

[persistence-rollout] `turn_aborted` rollout payload carries non-upstream `error_info` field (wrong-field)
  RUST: codex-rs/protocol/src/protocol.rs:3691-3703 `TurnAbortedEvent { turn_id, reason, completed_at?, duration_ms? }` — there is no `error_info` field. reason is constrained to TurnAbortReason (protocol.rs:3705-3712: interrupted/replaced/review_ended/budget_limited).
  SWIFT: Sources/Persistence/Rollout.swift:510-525 adds `payload["error_info"] = ei` to the turn_aborted event_msg. Reader reads it back at Rollout.swift:1077.
  DESC: Swift intentionally stores the fine-grained cause in a sibling `error_info` field. Upstream ignores unknown fields on read so this does not break parsing, but it is a non-canonical addition to the on-disk event shape. The canonical `reason` collapse to the four upstream variants is itself correct (abortReason map).
  FIX: Acceptable as a tolerated extension; if strict fidelity is required, drop `error_info` and accept the lossy reason collapse, or document this as a deliberate superset field.

[persistence-rollout] SessionMeta written with hardcoded source/originator/cli_version/model_provider; missing forked_from_id and agent_* fields (wrong-default)
  RUST: codex-rs/rollout/src/recorder.rs:673-693 SessionMeta is built from real values: originator()=`{originator}/{version}`, cli_version=CARGO_PKG_VERSION, source=actual SessionSource, plus forked_from_id, agent_nickname, agent_role, agent_path, thread_source, model_provider from config. protocol.rs:2720-2751 declares all these fields.
  SWIFT: Sources/Persistence/ThreadStore.swift:153-176 hardcodes cliVersion="0.1.0", originator="codex-swift/0.1.0", source="vscode", modelProvider="openai"; the .sessionMeta case (Rollout.swift:44-54, 560-588) has no slots for forked_from_id, agent_nickname, agent_role, agent_path, or thread_source, so they are never emitted.
  DESC: The session_meta first line is shape-compatible (snake_case field names, payload-flattened, written first) but its values are placeholders and several upstream fields are absent. forked_from_id absence in particular means fork lineage is not recorded on disk; agent_* absence means sub-agent rollouts cannot be identified by upstream tooling. All are Option/skip-if-none upstream so deserialization still succeeds.
  FIX: Plumb real model_provider/source through SessionConfig, and add optional `forked_from_id`, `agent_nickname`, `agent_role`, `agent_path`, `thread_source` to the .sessionMeta case + writer so fork/sub-agent lineage is faithfully persisted.

[config] config version string format differs (mtime-size vs sha256 of canonical JSON) (wrong-semantics)
  RUST: config/src/fingerprint.rs:35-47 version_for_toml hashes the canonical JSON of the user layer TOML and returns "sha256:{hex}"; this is the version returned in ConfigWriteResponse.version and ConfigLayerMetadata.version.
  SWIFT: Sources/Supervisor/RequestRouter.swift:5997-6004 configVersion returns "{modifiedMs}-{size}" from file attributes (or "0").
  DESC: The version token format is part of the wire contract (prefix sha256:, content-derived). Swift uses a filesystem mtime-size token. Because expectedVersion round-trips through the same server it remains internally consistent, but any client that inspects/compares/displays the version or expects content-stable hashing (e.g. equal content => equal version across machines) will diverge.
  FIX: Compute version as sha256 of the canonical (sorted-key) JSON of the user-layer TOML and prefix with "sha256:", matching version_for_toml, for both config write responses and origins/layer metadata.

[config] find_project_root fallback walks to filesystem root when no marker is present (wrong-semantics)
  RUST: config/src/loader/mod.rs:1070-1092 find_project_root returns cwd itself when no ancestor contains a project_root_marker (so load_project_layers only considers cwd's .codex).
  SWIFT: Sources/Config/Config.swift:507-529 the ancestor loop only breaks on a marker match; with no marker anywhere it appends every ancestor up to filesystem root, so .codex/config.toml in any ancestor dir is loaded.
  DESC: In a directory tree with no marker (e.g. no .git anywhere up the path), upstream restricts project-local discovery to cwd only, whereas Swift loads .codex/config.toml from every ancestor up to /. Edge case, but it can pull in unintended ancestor configs.
  FIX: When the marker walk reaches filesystem root without a match, restrict the project-local layer set to cwd only (mirror find_project_root returning cwd), instead of including all ancestors.

[prompts] Personality migration not ported (missing-method)
  RUST: codex-rs/core/src/personality_migration.rs:15-63 `maybe_migrate_personality` + `PERSONALITY_MIGRATION_FILENAME = ".personality_migration"` writes a marker and, for existing-session users without an explicit personality, persists `Personality::Pragmatic` via ConfigEditsBuilder.
  SWIFT: ABSENT — grep for `personality_migration`, `PersonalityMigration`, `maybe_migrate_personality`, `SkippedMarker` across Sources/ returns no matches.
  DESC: Upstream performs a one-time config migration that pins existing users to the pragmatic personality (writing a `.personality_migration` marker in codex_home). Swift does not perform this migration, so an upgraded user's effective personality default could differ from upstream. This is config-state, not a wire method, so impact is limited.
  FIX: Port personality_migration.rs as a config-side migration run at startup against codex_home (write `.personality_migration` marker; set personality=pragmatic when no explicit personality and prior sessions exist). Low priority unless personality-default parity is required.

[prompts] Realtime backend system prompt (realtime_prompt.rs) not ported (prompt-text)
  RUST: codex-rs/core/src/realtime_prompt.rs:1-24 `prepare_realtime_backend_prompt` loads templates/realtime/backend_prompt.md (`## Identity, tone, and role` / `You are Codex, an OpenAI general-purpose agentic assistant...`), trims, and substitutes `{{ user_first_name }}` from whoami realname/username (default `there`).
  SWIFT: ABSENT for backend_prompt — Sources/Prompts/Fragments.swift:333-364 only ports the realtime_context start/end fragments (realtime_start.md / realtime_end.md), not the backend system prompt. The `{{ user_first_name }}` substitution and whoami fallback are not implemented. Realtime methods (thread/realtime/*) are in the unsupported list (ProtocolModel/GenericResponses.swift:199-200).
  DESC: The full realtime model system prompt and its user-first-name substitution are not reproduced. Because realtime methods are currently unsupported in the Swift router, this has no live wire impact today, but it is a gap relative to the named audit unit.
  FIX: When realtime support is implemented, vendor templates/realtime/backend_prompt.md verbatim and reproduce `prepare_realtime_backend_prompt` (config-override > request-prompt > default with `{{ user_first_name }}` -> first word of full name/username, fallback `there`).

[mcp] initialize clientInfo name differs from upstream codex-mcp-client identity (wrong-semantics)
  RUST: codex-mcp/src/rmcp_client.rs:483-490 sets client_info Implementation { name: "codex-mcp-client", version: CARGO_PKG_VERSION, title: Some("Codex") }.
  SWIFT: Sources/MCP/McpClient.swift:668 and McpHttpClient.swift:667 send clientInfo `["name": "CodexKit", "version": "0.1"]` with no title.
  DESC: The clientInfo advertised to MCP servers is `CodexKit`/`0.1` rather than upstream's `codex-mcp-client` with title `Codex`. Most servers ignore client identity, but servers that key behavior, allowlists, or telemetry off the client name will see a different identity than the reference implementation.
  FIX: Send clientInfo name `codex-mcp-client` (and title `Codex`) to match upstream, or document the intentional rename if frontend telemetry depends on CodexKit.

[hooks] PreToolUse continue:false is treated as a block instead of an unsupported/failed output (wrong-semantics)
  RUST: hooks/src/engine/output_parser.rs:319-329 `unsupported_pre_tool_use_universal`: if `!continue_processing` returns invalid_reason "PreToolUse hook returned unsupported continue:false"; events/pre_tool_use.rs:214-219 sets status Failed and does NOT block when invalid_reason is present.
  SWIFT: Sources/HarnessCore/Hooks.swift:1148-1155 — the generic JSON branch in runCommand applies `if obj["continue"]==false { decision = .block }` for every event, with the Stop-specific shouldStop only added inside; for PreToolUse this yields decision=.block (a hook block) rather than a failed/ignored hook.
  DESC: Upstream rejects `continue:false` (and `stopReason`/`suppressOutput`) on PreToolUse as unsupported, failing the hook run without blocking the tool. Swift's generic parser converts `continue:false` into a tool block for PreToolUse (and PermissionRequest, PostToolUse, UserPromptSubmit), diverging from upstream which only honors `continue:false` as a session-terminator on Stop and as an error elsewhere.
  FIX: In runCommand, gate the `continue:false -> .block` mapping by event: only Stop should map continue:false to shouldStop; for PreToolUse/PermissionRequest treat continue:false (and stopReason/suppressOutput) as a schema violation surfaced via `outputSchemaError` with the verbatim upstream text, and do not block.

[hooks] PostToolUse/PermissionRequest unsupported-output invalid_reason messages not surfaced (wrong-semantics)
  RUST: hooks/src/engine/output_parser.rs:343-364 `unsupported_post_tool_use_universal` ('PostToolUse hook returned unsupported suppressOutput'), `unsupported_post_tool_use_hook_specific_output` ('...unsupported updatedMCPToolOutput'), and permission-request reserved-field rejections ('...unsupported updatedInput/updatedPermissions/interrupt:true') -> set HookRunStatus::Failed.
  SWIFT: Sources/HarnessCore/Hooks.swift:1039-1041 PostToolUse/SessionStart/UserPromptSubmit branch only reads additionalContext; PermissionRequest branch :1029-1038 only reads decision.behavior/message. The reserved/unsupported fields (`updatedMCPToolOutput`, `updatedInput`, `updatedPermissions`, `interrupt`, `suppressOutput`) are silently ignored; no `outputSchemaError` set.
  DESC: Upstream marks these reserved/unsupported fields as invalid (failing the hook run with a specific message) so PermissionRequest hooks 'fail closed'. Swift ignores them, so e.g. a PermissionRequest hook that sets `interrupt:true` or `updatedInput` is silently accepted rather than failing closed. Low traffic but a correctness/safety divergence in the permission path.
  FIX: In parseHookSpecificOutput, detect these reserved fields for PostToolUse (`updatedMCPToolOutput`, universal `suppressOutput`) and PermissionRequest (`updatedInput`/`updatedPermissions`/`interrupt`), set `outputSchemaError` to the verbatim upstream strings, and for PermissionRequest suppress the decision (fail closed) when present.

[auth] RateLimitWindow.usedPercent emitted as float passthrough instead of rounded integer (wrong-semantics)
  RUST: v2/account.rs:337-353 RateLimitWindow.used_percent: i32 with `used_percent: value.used_percent.round() as i32`; core RateLimitWindow.used_percent is f64 (protocol.rs:2105).
  SWIFT: Sources/Supervisor/RequestRouter.swift:1229 sets "usedPercent": object["used_percent"] ?? .null, passing the backend value through unchanged (a float).
  DESC: The V2 wire field usedPercent is an integer (f64 rounded to i32). Swift forwards the raw backend used_percent, which can be a non-integer float (e.g. 45.7), diverging from the integer the frontend expects.
  FIX: In rateLimitWindow(), round used_percent to the nearest integer before emitting (e.g. .int(Int64(d.rounded()))), matching v2/account.rs:348.

[auth] getAuthStatus never returns requiresOpenaiAuth:false for providers that don't require OpenAI auth (wrong-semantics)
  RUST: account_processor.rs:751-758 sets requires_openai_auth = self.config.model_provider.requires_openai_auth and returns GetAuthStatusResponse{auth_method:None, auth_token:None, requires_openai_auth:Some(false)} when the provider does not require auth.
  SWIFT: Sources/Supervisor/RequestRouter.swift:2608-2642 handleGetAuthStatusRequest never consults the provider; every return path passes requiresOpenaiAuth: true.
  DESC: For a custom model provider with requires_openai_auth=false, upstream getAuthStatus reports requiresOpenaiAuth:false and authMethod:null. Swift always reports requiresOpenaiAuth:true, telling the frontend auth is required when it is not. (account/read at RequestRouter.swift:3576 does gate on currentProviderRequiresOpenAIAuth(), so only getAuthStatus is affected.)
  FIX: In handleGetAuthStatusRequest, short-circuit to authStatusJSON(authMethod:nil, authToken:nil, requiresOpenaiAuth:false) when currentProviderRequiresOpenAIAuth() is false, mirroring account_processor.rs:753-758.

[auth] Missing external-auth-active and forced_login_method login guards (missing-error-code)
  RUST: account_processor.rs:245-249 external_auth_active_error; login_api_key_common (255-266) and login_chatgpt_common (310-318) return invalid_request with messages "External auth is active...", "API key login is disabled. Use ChatGPT login instead.", "ChatGPT login is disabled. Use API key login instead."; auth manager forced_login_method at login/src/auth/manager.rs:612,631-641.
  SWIFT: ABSENT - grep for forcedLoginMethod / is_external_chatgpt_auth_active / "External auth is active" / "login is disabled" across Sources/ returns nothing; the account/login/start dispatch (RequestRouter.swift:1894-2016) performs none of these checks.
  DESC: When external chatgptAuthTokens auth is active, or when config forces a specific login method, upstream rejects the wrong login type with a specific invalid_request error. Swift performs no such gating, so an apiKey/chatgpt managed login while external auth is active will proceed and clobber state instead of being rejected.
  FIX: Add an is-external-chatgpt-active check (AuthManager already knows tokenType=="BearerExternal") at the start of the apiKey/chatgpt/chatgptDeviceCode branches returning invalid_request with the upstream message; and honor a forced_login_method config to reject the disallowed login type with the corresponding message.

[auth] Cancel completion notification uses error text "canceled" instead of upstream "Login was not completed" (prompt-text)
  RUST: account_processor.rs:466-469 device-code cancel branch produces (false, Some("Login was not completed")) and account_processor.rs:478-487 sends AccountLoginCompletedNotification{success:false, error:Some("Login was not completed")}.
  SWIFT: Sources/Supervisor/RequestRouter.swift:2052-2053 sends accountLoginCompleted(loginId:..., success:false, error:"canceled") directly from the account/login/cancel handler.
  DESC: On login cancellation upstream emits account/login/completed with error "Login was not completed"; Swift emits the literal "canceled". A frontend that displays or branches on this error string will see different text.
  FIX: Change the cancel-path AccountLoginCompleted error string at RequestRouter.swift:2053 from "canceled" to "Login was not completed" to match account_processor.rs:468.